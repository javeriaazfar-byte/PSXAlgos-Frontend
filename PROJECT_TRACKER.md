# Project Tracker

> Last updated: 2026-05-18 (**fix(strategy-builder): preset chip concatenation — `PresetChipStrip` now uses `commitExpression` (token-at-caret replacement) instead of `insertAtCaret` (raw cursor insert), preventing `50atr_percent` / `atrbb_percent_b` style invalid tokens.**) Previous: 2026-05-15 (**SB10 frontend addition — volatility-context preset chips (ATR %, Distance (ATR), Bollinger %B, BB Bandwidth) now one-tap insertable in `<ExpressionInput>`.** Frontend-only preset layer over SB1 + SB8 — **no backend changes**. Single-file diff at `app/components/strategy-editor/expression/expression-input.tsx` (+117 lines): `PRESET_CHIPS` module-scope catalog (four `PresetChip` records with `id` / `label` / `description` / `expression` / `matchKeys`); a new `<PresetChipStrip>` toolbar rendered above the input on all viewports (desktop + mobile — wider chips than `<OperatorChipStrip>` so labels like "BB Bandwidth" fit; 44 px touch-target min-height; `title` + `aria-label` carry the description for a11y); and an autocomplete insertion loop that emits one suggestion per matching preset with `hint: "preset"`. Empty token surfaces all four; otherwise `matchKeys.some(k => k.startsWith(query))`. Two bare-identifier presets (`atr_percent`, `bb_percent_b`) ride the backend TA service's existing publish paths; two genuine arithmetic compositions — `(close_price - sma_50) / atr` (Distance in ATR units; negative = below MA, positive = above) and `(bb_upper - bb_lower) / bb_middle` (BB bandwidth as a fraction of the midline; volatility-regime indicator with no equivalent single-name indicator). On commit the chip's `onClick` calls the existing `insertAtCaret(preset.expression)` seam — same contract as `<OperatorChipStrip>` (inserts at the current selection range, overwrites the user's selection by design); the SB1 parser canonicalizes on save and the `value_source` field on `SingleCondition` preserves user-authored text so save→reopen yields the same string. **Tester PASS**: `./node_modules/.bin/tsc --noEmit` clean (exit 0); `npm run lint` (= `tsc --noEmit` + `scripts/check-env.mjs`) clean (`check-env: OK — no NEXT_PUBLIC_* names look sensitive`); `npm run build` clean (`✓ Compiled successfully in 17.1s`, `Finished TypeScript in 9.9s`, all 23 static pages generated, no warnings — includes `/strategies/[id]` and `/strategies/new`). Preset insertion smoke executed against the actual `tryParseExpression` / `expressionToSource` code path the chip onClick invokes downstream (transpiled from `lib/strategy/expression.ts` via `tsc`): all 4 chips on empty input pass exact-canonical round-trip (`tryParseExpression(...).canonical === preset.expression`, caret at `text.length`); mid-caret realistic scenario (`"2 * "` then tap `ATR %` → `"2 * atr_percent"`, caret 15) round-trips canonically; autocomplete with token `"atr"` returns `["ATR %", "Distance (ATR)"]` (both legitimate prefix hits on `atr_percent` and `atr_dist`); empty-token autocomplete surfaces all 4 (`["ATR %", "Distance (ATR)", "Bollinger %B", "BB Bandwidth"]`); typing `"bandwidth"` returns `["BB Bandwidth"]` only; save→reopen round-trip simulated for all 4 (`value_source` stored as `preset.expression` verbatim, reparsing and re-rendering yields the same string, no drift across save/load). Backend round-trip via `python -c "from app.services.strategy.expression_parser import parse_expression; ..."` for all 4 expressions returned the expected `IndicatorRefNode` / `BinaryOpNode` shapes (no BE work needed — these are pure SB1+SB8 surface, which BE already supports). Playwright not exercised — editor route sits behind `proxy.ts` Google OAuth (matcher `/strategies/:path*`), no `playwright.config.*` or `playwright` dependency in `app/`; tester elected to exercise the contract via the actual production code path rather than introduce browser + auth fixtures for a single-file UI change. ADR-009 in `psxDataPortal/docs/ARCHITECTURE.md` amended with the SB10 preset-layer note. SB10 dossier at `psxDataPortal/docs/design_call_dossiers/SB10_volatility_presets_2026-05-15.md`. **Validates the ADR-009 thesis**: SB10 is the first non-backend SB capability to ship purely on the SB1+SB8 surface — "SB3/SB8/SB10/SB11 ship as pure indicator/UI work" is now empirically true. Previous Last-updated narrative — **SB8 frontend mirror — math helpers `abs`, `max`, `min`, `round`, `log` now authorable in `<ExpressionInput>`.** Companion to the SB8 backend sweep on `psxDataPortal` (shipped same day, single coordinated commit pair). Three files touched, all mechanical TS ports of the backend Python (~150 LOC). `app/lib/api/strategies.ts` adds `MathFnName = "abs" | "max" | "min" | "round" | "log"` and `FunctionCallNode` (`type: "function_call"`, `name: MathFnName`, `args: ExprNode[]`) to the `ExprNode` union — the discriminated-union pattern stays identical to the SB1 five variants; the editor's many import sites keep working since `ConditionValue` is already re-aliased to `ExprNode`. `app/lib/strategy/expression.ts` adds the `MATH_FN_NAMES` whitelist set (`{abs, max, min, round, log}` — exported so the autocomplete in `<ExpressionInput>` can iterate it) and a private `MATH_FN_ARITY` map mirroring backend `_MATH_FN_ARITY` (`{abs:(1,1), round:(1,2), log:(1,2), max:(2,null), min:(2,null)}`); `parseIndicatorRef` renamed to `parseIdentPrimary` with the math-fn-precedence branch (`MATH_FN_NAMES.has(name) && peek().kind === "LPAREN"` → `parseFunctionCallTail`); render / nodePrec / walkDepth / walkAndCheck / hasIndicatorRef each gain a `function_call` case matching the backend exactly. `app/components/strategy-editor/expression/expression-input.tsx` autocomplete (`:177-185`) iterates `MATH_FN_NAMES` and surfaces `abs( … )` / `max( … )` / `min( … )` / `round( … )` / `log( … )` snippets with `hint: "math fn"` when the current token is empty or prefix-matches a fn name. **Verification (tester PASS):** `npx tsc --noEmit` clean, `npm run lint` clean (`tsc --noEmit && node scripts/check-env.mjs`), `npm run build` clean (Next 16.2.4 + Turbopack, 17.5 s compile, full route table). No new Playwright config — the 5 fns are wired statically and exercised by the FE build; backend dossier §7.7 mirror tests are deferred until psx-ui has a TS test runner (same posture as the SB1 Sweep 2 verification — Vitest N/A). The TS port is deliberate duplication: parsing in the browser gives users instant feedback (error caret under the field as they type) and the backend re-validates on save so the contract is symmetric. If/when a `/strategies/validate-expression` endpoint ever ships, the TS port can be retained as the latency-zero fast path or dropped — the API surface in the editor is small enough that either swap is local. **One spec hygiene note** (carried over from SB1 Sweep 2 and confirmed against the SB8 dossier §0.4): the original task #1 brief's "vectorized over `pd.Series`" line was stale — the SB1/SB8 evaluator is scalar; the vectorized form is forward-compat for SB10+. **Files**: 3 modified — `app/lib/api/strategies.ts` (+13 / -1), `app/lib/strategy/expression.ts` (+114 / -3), `app/components/strategy-editor/expression/expression-input.tsx` (+14 / -0). Companion backend SHA: shipped on `psxDataPortal main` same commit (`feat(strategy): SB8 — math helpers (abs, max, min, round, log) in expressions`). Dossier: `psxDataPortal/docs/design_call_dossiers/SB8_math_helpers_2026-05-15.md`. ADR-009 in `psxDataPortal/docs/ARCHITECTURE.md` amended with the SB8 extension note. **Unlocks SB10 (volatility-context expressions) and SB11 (statistical functions) on the editor side: users can now type `abs(close_price - sma_20) / atr_14 < 0.5` directly.**)

> Previous (2026-05-15) (**SB1 Sweep 2 — Free-form arithmetic expression editor landed on condition leaves.** The strategy editor's RHS value picker is now a single-line free-form expression input with autocomplete and a mobile operator chip strip, replacing the legacy two-mode "Constant vs Indicator" segmented control. Industry-standard parity with TradingView Pine Script + TradingView Screener intellisense; matches the SB1 backend AST shape shipped earlier today (`psxDataPortal` commit `f1b06b3` — `ConditionValue` widened from `Union[ConstantValue, IndicatorValue]` to a five-variant `ExprNode` discriminated union: `ConstantNode | IndicatorRefNode | BinaryOpNode | UnaryOpNode | ParenNode`). **Mini Pratt parser** at `app/lib/strategy/expression.ts` is a line-for-line TS port of `psxDataPortal/backend/app/services/strategy/expression_parser.py` — same tokens, same binding powers, same error column format. Public surface: `parseExpression(source) -> ExprNode` (throws `ParseError`), `tryParseExpression(source)` non-throwing variant returning `{ok: true, ast, canonical}` or `{ok: false, message, column, reason, path}` (`reason` ∈ `parse_error | comparison_op_in_arith | div_by_literal_zero | depth_overflow | parameterized_ref_deferred`, mirroring the four backend `expression_validator.py` reason codes), `expressionToSource(ast) -> string` (canonical render), `validateExpression(node)`, `hasIndicatorRef(node)`, plus exported `KNOWN_INDICATORS` Set + `MAX_EXPR_DEPTH=16` constant. Numbers render via `renderNumber` that trims trailing zeros from floats so `5.00` → `5` and `5.50` → `5.5`. Round-trip property holds: 15/15 smoke cases (bare indicator / complex arith `(close_price - sma_50) / atr` / trailing-operator parse_error / empty input no-crash / constant int+float / unknown indicator rejection / all 4 reason codes / canonical render spacing / unary minus / round-trip-via-canonical) pass via Node against the transpiled module. **Type mirror** at `app/lib/api/strategies.ts:38-95` widened from the two-variant union to the five-variant `ExprNode` discriminated union (`ArithOp` `+ - * / %`, `CmpOp` `< > <= >= == !=`); `ConditionValue` is re-aliased to `ExprNode` so the editor's many import sites keep working, and `ConstantValue` / `IndicatorValue` retained as typing-level aliases for any external consumer. `SingleCondition.value_source?: string | null` added — user-authored source text persisted so the editor re-opens with exact whitespace + parens; backend backfilled via migration 058. **`<ExpressionInput>` component** at `app/components/strategy-editor/expression/expression-input.tsx` — headless controlled single-line input with debounced (150ms) `tryParseExpression` on every keystroke, autocomplete dropdown rendered inline below the input (position-aware: identifiers + operators offered when cursor follows operand or starts at empty; operands offered after a binary operator), keyboard navigation (↑/↓ to move highlight, Enter / Tab to commit the token replacing the current identifier-or-numeric span via `[start, end]` caret detection, Esc to dismiss). Successful parse shows a faint "canonical" preview when the source differs from `expressionToSource(ast)` (informational only — not auto-applied; preserves user-authored whitespace). Mobile-only `OPERATOR_CHIPS` strip (`+ − × ÷ % ( ) .` — comparators intentionally excluded since `SingleCondition.operator` owns the boolean comparison) rendered above the input when `useTouchPointer` detects `(hover: none) and (pointer: coarse)`; `showOperatorChips` prop overrides to `"always"` / `"never"` for tests. Tapping a chip inserts its literal at the current caret position via direct `setSelectionRange` manipulation. ARIA: `role="combobox"` + `aria-expanded` + `aria-autocomplete="list"` + `aria-activedescendant` on the input; `role="listbox"` on dropdown with `role="option"` + `aria-selected` per row; `role="toolbar"` `aria-label="Expression operators"` on chip strip with per-button `aria-label` ("Plus", "Open parenthesis", etc.); `aria-invalid` driven by debounced parse + `forceInvalid` escape hatch for save-attempt shake. **Drawer refactor in `editor-view.tsx`** (lines `app/app/strategies/[id]/editor-view.tsx`): the legacy `compareMode` / `threshold` / `refIndicator` triple-state was replaced with a single `expressionText: string`; the "compared to" segmented control + custom slider + ref-indicator Combobox (~150 lines) was deleted and replaced with one `<ExpressionInput>`; the diff shows `-296 / +141` net (-218 lines on `editor-view.tsx`). `handleSave` calls `tryParseExpression(expressionText)` one final time before emitting — on `ok: true` it writes `{value: result.ast, value_source: expressionText}` (the exact user text, not the canonical), on `ok: false` it sets `forceInvalid=true` to shake the input and does NOT close the drawer. Read-site helpers updated: `formatValue` now delegates to `expressionToSource(value)` (dropped the inline branch on `value.type`); `condToMeta.valIsRef` now uses `hasIndicatorRef(value)` (recursive walk — true if any `IndicatorRefNode` appears anywhere in the expression, so a canvas leaf with `close - sma_50` correctly flags as containing a live series); `describeCondition` uses `expressionToSource`. New-leaf default at `editor-view.tsx` seeds both `value: {type: "constant", value: 50}` and `value_source: "50"`. **Wizard preset** at `app/app/strategies/new/page.tsx` also seeds `value_source` on the preset-generated condition leaves so the "blank" + "mean-reversion" preset paths produce strategies that hydrate cleanly into the new editor. **Verification (tester PASS):** `npx tsc --noEmit` clean; `npm run lint` (= tsc --noEmit + check-env.mjs) clean; `npm run build` succeeds (Next 16.2.4 + Turbopack, 23 routes, 19.7s compile, 7.7s TS, 393ms static gen). Vitest N/A — psx-ui has no test runner setup yet (deferred follow-up per the Phase-5 InheritableField note from 2026-05-07). 15/15 parser smoke tests PASS via Node against the transpiled `expression.ts`. Round-trip stash protocol confirmed the new component is genuinely wired (`tsc --noEmit` fails on missing imports at `editor-view.tsx:55-56` when the new files are stashed, plus 3 implicit-any complaints at the JSX site — exactly the right failure shape). Playwright visual smoke was unrunnable in the test agent's MCP sandbox (`ERR_CONNECTION_REFUSED` against `:3100` from inside the browser sandbox); the spec §9 flow is documented in `psxDataPortal/docs/design_call_dossiers/SB1_frontend_expression_editor_2026-05-15.md` and gates on the next Vercel preview. **One spec hygiene note:** the task description's example prompts `close > sma_50` and `(close - sma_50) / atr_14 < 0.5` reflect stale grammar — SB1 `ExpressionInput` is arithmetic-only (the comparison operator lives in the outer `SingleCondition.operator`, not inside the expression value; grammar comment at `expression.ts:6-18`; reason code at `:454`). Canonical tokens are `close_price` / `sma_50` / `atr`, not `close` / `atr_14`. The smoke uses the canonical tokens and confirms `comparison_op_in_arith` correctly fires on the task-description prompts — i.e. the parser is behaving exactly as designed; the task description was just out of date. **Files**: 2 new (`app/components/strategy-editor/expression/expression-input.tsx` 492 lines, `app/lib/strategy/expression.ts` 486 lines), 3 modified (`app/app/strategies/[id]/editor-view.tsx` net -155, `app/app/strategies/new/page.tsx` +10 / -0, `app/lib/api/strategies.ts` +46 / -7). Companion backend ADR-009 at `psxDataPortal/docs/ARCHITECTURE.md:992` amended with the frontend implementation note. Implementation spec at `psxDataPortal/docs/design_call_dossiers/SB1_frontend_expression_editor_2026-05-15.md`.)

> Previous (2026-05-15) (**BT1-BT4 — Backtest results page now surfaces every metric the API already returns + adds drawdown subchart and trade analytics row.** Industry-standard parity with TradingView Strategy Tester, zero backend changes, zero new dependencies. All four sections live in `app/app/backtest/backtest-view.tsx`. **BT1 — Risk & distribution tiles**: second 6-up Lede grid renders below the original 6-tile Lede (lines 557-623) exposing `sortino_ratio`, `cagr`, `volatility`, `max_drawdown_duration`, `avg_win`, `avg_loss`, `largest_win`, `largest_loss` — all already returned in `BacktestResultResponse` but previously dropped. Null-safe via `== null → "—"` short-circuit; `fmtPctSigned` / `fmtPctAbs` / `fmtPkr` formatters (lines 44-65) short-circuit on `!Number.isFinite` so Infinity / NaN render as "—". **BT2 — Methodology warnings banner**: `<WarningsBanner warnings={result.warnings} />` (line 500, component defined at line 1076) renders above the equity curve when `result.warnings` is non-empty. Surfaces survivorship-bias / sparse-data / look-ahead caveats the backend was already emitting but the UI was silently dropping. Banner guarded by `result.warnings && result.warnings.length > 0` so absent-warnings runs render no chrome. **BT3 — Drawdown subchart**: synchronized `<EquityCurve>` instance directly under the equity curve (lines 697-716) bound to a `drawdownValues` series mapped from `equity_curve[].drawdown`, rendered in `T.loss` color with `grid={false}` and 74px height so it reads as a subordinate pane. Guarded by `drawdownHasMovement` (line 397 — `drawdownValues.length > 1 && drawdownValues.some(v => v !== 0)`) so it's hidden when the series is empty or flat-zero. **BT4 — Trade analytics row**: 3-column grid (lines 885-912) renders `<PnLHistogram>`, `<HoldTimeHistogram>`, `<ExitReasonDonut>` (component definitions at lines 1142, 1228, 1297) below the trade-log filter strip. All three derive from `result.trades` — no backend changes. Empty trades short-circuit to an `<EmptyAnalyticsBody label="no trades yet" />` placeholder for each chart so the row never crashes. **API surface** (`app/lib/api/strategies.ts:474-485`): `BacktestResultResponse` extended with optional `warnings: Array<{code, severity: "info"|"warning", message, details?}>` typed to match the backend `warnings[]` array shape. **Verification**: tester PASS — `npx tsc --noEmit` clean, `npm run lint` clean, `npm run build` clean (23 static pages, `/backtest` and `/backtest/new` compile); `git stash` round-trip reproduced pre-fix state (0 BT markers) and `git stash pop` re-applied (8 BT markers). Visual smoke skipped per spec since `/backtest` is auth-gated via NextAuth Google OAuth.)

> Previous (2026-05-08) (**Editor canvas — spine tightened to 160px on each side of Risk Defaults.** User: *"too much space in the centre, we should move them closer I guess in the middle"*. The previous symmetry fix set both gaps to 402px which made the canvas read as a wide dead zone with the hub floating in space. Collapsed both `ROOT_TO_OUTPUT_GAP` and `RISK_TO_EXIT_GAP` to a single `SPINE_GAP = 160` constant in `app/app/strategies/[id]/editor-view.tsx` (both `resetView` and the render block). 160px is large enough that the bezier's 80px control-point offset still produces a visible curve (not a degenerate horizontal) but small enough that the hub sits visually attached to both trees rather than floating between them. Docs: `EXITS_OPTION_C_NOTES.md` spine-symmetry paragraph extended with the second-pass tightening rationale. Verification: `tsc --noEmit` clean; `npm run build` succeeds (36 routes).)

> Previous (2026-05-08) (**Editor canvas — spine is now symmetric around Risk Defaults.** User: *"why aren't both side symmetrical though, like left is too far and right too near"*. The entry-side gap (entry root gate right edge → Risk Defaults left pin) was `ROOT_TO_OUTPUT_GAP = 402`, but the exit-side gap (Risk Defaults right pin → exit root gate left edge) was only `RISK_TO_EXIT_GAP = 80` — visually the hub sat far right of center, the entry tree floated out to the left, the exit tree hugged the hub. Fix is one line in two places (`resetView` and the render block) in `app/app/strategies/[id]/editor-view.tsx`: `RISK_TO_EXIT_GAP = ROOT_TO_OUTPUT_GAP` so both wire spans match. After the change, Risk Defaults sits at the geometric center of the wire spine and the canvas reads as a balanced hub. Docs: `EXITS_OPTION_C_NOTES.md` got a "Phase 4b spine-symmetry fix" paragraph documenting the gap pairing. Verification: `tsc --noEmit` clean; `npm run build` succeeds (36 routes). Visual confirmation gates on the next Vercel preview of `psx-algos.vercel.app/strategies/22`.)

> Previous (2026-05-08) (**Editor canvas — exit-tree wires no longer slice across gate glyphs.** User screenshot showed the dashed leaf wires and solid nested-group wires on the right (exit) side terminating at the WRONG side of every AND/OR gate glyph and slicing across them — visible as wire segments crossing right through the AND glyph and the OR glyph in the screenshot, "the lines are all over the AND / OR gates, unlike entry side where they stop". Root cause was geometric: in the entry tree the gate's LEFT edge faces children (input side) and RIGHT edge faces parent (output side); after `mirrorLayout` reflects the exit tree the convention flips — gate's RIGHT edge now faces children, LEFT edge faces parent — but `buildVisit` in `app/app/strategies/[id]/editor-view.tsx` was reusing the entry-tree convention for both trees (`parent.gateX` for input, `n.gateX + GATE_W` for output), so every wire on the exit side terminated on the far side of its target gate and had to slice across the glyph to get there. Fix is local and small: `buildVisit` now takes a `mirrored: boolean` flag; two helpers `groupOutputX(g)` and `groupInputX(g)` pick the correct edge per tree (`mirrored ? gateX : gateX + GATE_W` for output, the inverse for input). Leaves were already fine — `mirrorLayout` had reflected `pinX` to the leaf's left edge, which is the side that faces the parent gate after mirror, so `n.pinX` is correct for both trees. Tree structure is unchanged (depth-from-root layered layout retained from yesterday's revert) — this fix only adjusts where the bezier endpoints LAND, not where any node sits. Docs: `EXITS_OPTION_C_NOTES.md` got a new "Phase 4b wire-side fix" paragraph documenting the side-of-gate convention per tree. **Verification**: `tsc --noEmit` clean; `npm run build` succeeds (36 routes). Visual confirmation gates on the next Vercel preview of `psx-algos.vercel.app/strategies/22`.)

> Previous (2026-05-08) (**Editor canvas — STRATEGY hub treatment landed.** Follow-up to the morning's alignment fix: the user asked for "something in center like some symbol or box, and then both sides have nodes, right for exit and left for entry". The dual-tree geometry was already correct — what was missing was a clear visual identity on the central node so it reads as THE strategy anchor and not just one of three peer cards. Changes in `app/components/strategy-editor/risk-defaults-node.tsx`: (1) **Hub header bar** added at the top of the card — `← Entry · STRATEGY · Exit →` (mono uppercase, primary tint, space-between layout) so the directional flow is communicated inline by the hub itself. (2) **Primary-tinted ring + soft glow** via layered `boxShadow` (`0 0 0 1px primary55, 0 0 0 4px primary10, 0 6px 18px -10px ...`) so the central card visually elevates above the surrounding tree leaves. (3) **Larger header icon** (22px → 26px badge with the ⚖ glyph) for stronger visual presence. The free-floating `BranchLabel` "ENTRY" / "EXIT" pills above each root gate were dropped — the hub's inline cue does the same job without two redundant labels pulling attention away from the spine. The `BranchLabel` component definition was deleted entirely (no remaining callers). Docs: `EXITS_OPTION_C_NOTES.md` `RiskDefaultsNode` row updated to call out the hub header + ring; the `BranchLabel` bullet was reframed as "direction reads from the hub itself". **Verification**: `tsc --noEmit` clean; `npm run build` succeeds (36 routes). Visual confirmation gates on the next Vercel preview deploy.)

> Previous (2026-05-08) (**Exits Phase 4b alignment fix — Y-aligned roots + dual-pin Risk Defaults landed.** The Phase 4b mirrored-exit-tree canvas shipped 2026-05-07 read as visually disjointed because each tree packs its own children top-down independently, so the entry root gateY (≈154 for a 2-leaf entry) and the mirrored exit root gateY (≈184 for a 1-leaf-plus-OR-group exit) sat at different Ys, and `RiskDefaultsNode` sat as a one-sided junction with a connector from the entry side only — making the right half of the canvas read as "unplugged". This fix ships three coordinated changes: (1) **`shiftLayoutY(layout, delta)` helper added to `app/lib/strategy/layout.ts`** as the Y-axis sibling of `mirrorLayout` — translates every node's `y` / `pinY` / `gateY` / `addSlotCy` (and recurses into children) by `delta`, no-op when `delta === 0`. (2) **Y-alignment in `editor-view.tsx`** render block + `resetView`: lay out both trees, mirror the exit tree about `mirrorAxisX`, then compute `unifiedGateY = max(entryRoot.gateY, exitRoot.gateY)` and shift the smaller tree down by `unifiedGateY - tree.gateY` so both root gates land on one horizontal line. `riskPinY` is now `unifiedGateY` (no longer `entryRoot.pinY`), so all three spine elements — entry gate, Risk Defaults pin, exit gate — share one Y. The mirrored exit tree's "incoming" point is `exitRoot.gateX/gateY` (which equals `exitRoot.pinX/pinY` after mirror, since pinX flipped from gateX+GATE_W to the new gateX). (3) **Right-side pin on `RiskDefaultsNode`** at `(NODE_W - 4, NODE_PIN_OFFSET_Y)` so the canvas can wire a connector from `(outputX + RISK_NODE_W, riskPinY) → (exitRoot.gateX, exitRoot.gateY)` matching the entry-side connector in style (primary color, 1.4 width). After Y-alignment both connectors are degenerate horizontals — the canvas now reads as one continuous spine. **Why bullet 6 in `EXITS_OPTION_C_NOTES.md` flipped**: the original Phase 4b shipped with "no central wire from exit tree to RiskDefaultsNode" on the theory that a second wire would clutter; in practice the canvas read as broken and reviewers asked "why does Risk only connect to one side?" Risk caps actually do govern both sides at runtime, so the dual wire is honest about what the node does — and combined with Y-alignment it adds geometry while removing visual confusion. **Files**: `app/lib/strategy/layout.ts` (+`shiftLayoutY`), `app/components/strategy-editor/risk-defaults-node.tsx` (+second `<Pin>`), `app/app/strategies/[id]/editor-view.tsx` (render block + `resetView` rewrite + new connector), `EXITS_OPTION_C_NOTES.md` (header date, RiskDefaultsNode row, mirrored-layout bullet, dual-pin connector bullet, Y-alignment bullet, flipped why-pattern #6, verification follow-up). **Verification**: `tsc --noEmit` clean; `npm run build` succeeds (36 routes, no warnings). The visual alignment can be confirmed on the next Vercel preview deploy of `psx-algos.vercel.app/strategies/22`.)

> Previous (2026-05-07) (**Exits Phase 4b — Mirrored exit-tree authoring on the editor canvas landed.** The strategy editor at `app/app/strategies/[id]/editor-view.tsx` now authors the signal-based exit tree (`exit_rules.conditions`) on the right side of the canvas as a parallel mirror of the entry tree — answering the user's question on `psx-algos.vercel.app/strategies/22`: *"how do you add output condition though?"* The mirror is a coordinate post-processor (`mirrorLayout(layout, axisX)` added to `app/lib/strategy/layout.ts`) that flips X coords of an already-placed `GroupLayout` about a vertical axis: leaf x → 2*axisX - x - w; gateX → 2*axisX - gateX - GATE_W; pinX/addSlotCx → 2*axisX - x. The layout module's `place` recursion is unchanged — same recursive packing, same per-group "+ Add condition" slot, same `walkLeaves`/`walkGroups`/`collectSlots`/`layoutBounds` helpers walk a mirrored layout transparently because every coord is already filled in. **Geometry**: `EXIT_ROOT_GATE_X = outputX + RiskDefaultsNode.WIDTH + RISK_TO_EXIT_GAP` (= 410+104+402+220+80 = 1216), `mirrorAxisX = (EXIT_ROOT_GATE_X + 410 + GATE_W) / 2` solved so the entry root's left edge (410) reflects to land at 1216 after mirror. **State refactor**: `[exit, setExit]` split into a metadata-only `[exit]` (passthrough for any non-conditions/non-default_risk fields) plus a real `[exitTree, setExitTree]` seeded via `fromBackend(initialStrategy.exit_rules?.conditions ?? {kind:"group",logic:"AND",conditions:[]})`; `Selection` type extended with `source: "entry"|"exit"` so the drawer mutates the right tree; all mutation handlers (`handleAddCondition`/`handleAddGroup`/`handleSetGroupLogic`/`handleUngroupGroup`/`handleDeleteGroup`/`handleDuplicateNode`/`handleDeleteNode`/`performUngroup`/`performDeleteGroup`) now take a trailing `source` and route via a small `treeFor(source) → [tree, setter]` helper; `selectedLeaf`/`selectedGroup` look up nodes via `treeFor(selection.source)`; `groupModal` and `pendingPicker` carry source so a confirmation modal opened from the exit tree applies to the exit tree. **Connector geometry**: kept the existing entry root → RiskDefaultsNode pin connector unchanged. The exit tree intentionally has no central wire — risk caps apply to both branches but visually wiring both roots to the same pin would clutter without adding information. Wire bezier in `pushWire` made sign-aware (`midX = from.x + 80 * Math.sign(to.x - from.x)`) so mirrored exit-tree wires curve toward their parent gate instead of overshooting. **Branch labels**: small `BranchLabel` ("ENTRY" / "EXIT") sits above each root gate so the two `AND` glyphs aren't read as one continuous structure. **Validation asymmetry**: the entry tree must have ≥1 leaf to save (existing behavior); the exit tree may be emptied — "no signal-based exits, rely on risk defaults" is a valid configuration. `performDeleteGroup`/`handleDeleteNode` now short-circuit on the leaf-count check only when `source === "entry"`. **buildUpdateBody signature** changed to `(name, tree, exit, exitTree, riskDefaults)`; serializes `exit_rules.conditions = hasAnyLeaf(exitTree) ? toBackend(exitTree) : null` so an empty exit tree wires `null` rather than a structurally empty group (which the backend would interpret as "always True → exit on every bar"). The `normalizeWireGroup` import was dropped — the editor now builds the wire shape directly via `toBackend`. **resetView** updated to fit both trees into the auto-fit viewport. **Verification**: `tsc --noEmit` clean; `npm run build` succeeds (36 routes, no warnings). **Reused machinery**: same `ConditionDrawer` and `GroupDrawer` components handle both trees — `ConditionDrawer`'s "New condition" copy switches to "New exit condition" when `creating.source === "exit"`. **Docs updated**: `EXITS_OPTION_C_NOTES.md` — removed the Phase 4b deferred callout, documented the mirrored layout / source-routed selection / connector geometry / branch labels / validation asymmetry, added two new "why" patterns (mirrored exit tree reuses every existing component vs separate panel; no central wire from exit tree to risk pin avoids clutter without losing information).)

> Previous (2026-05-07) (**Exits Phase 8 — Frontend reference doc landed.** New `psx-ui/EXITS_OPTION_C_NOTES.md` is now the canonical reference for the Option C hybrid-exits work on this repo, matching the one-doc-per-work-area convention set by `STRATEGY_EDITOR_WIRING_PLAN.md` and `STRATEGY_TREE_PLAN.md`. Documents all five new components (`RiskDefaultsNode` and `StatusStrip` under `app/components/strategy-editor/`, `InheritableField` under `app/components/`, `InheritanceWarningModal` under `app/components/strategy-editor/`, plus the inline `EffectiveRiskPanel` inside `app/app/backtest/backtest-view.tsx`) with paths, sizing rules, and per-state behavior. Documents wire-in points: editor canvas (`app/app/strategies/[id]/editor-view.tsx` — `[exit, setExit]` + `[riskDefaults, setRiskDefaults]` state pair, `buildUpdateBody` writes both branches every save, `performSave` snapshots `initialStrategy.exit_rules?.default_risk` BEFORE the PUT and opens the per-bot picker on `inheritance_warnings`, autosave suppresses the modal); bot create wizard (`app/app/bots/new/page.tsx` — strategy-fetch effect captures `default_risk`, right-rail Preview surfaces effective values via an `eff()` resolver mirroring the backend); backtest run form (`app/app/backtest/new/backtest-new-view.tsx` — `useEffect` on `strategyId` fetches defaults, presets null'd to keep anchor-counter intact, four exit-risk fields render as `<InheritableField>`, `RunRail.riskLabel` folds inheritance with `↳` prefix); shared `universe-and-risk-fields.tsx` (optional `strategyDefaults` prop, four exit-risk inputs render as `InheritableField` when defaults present); result page (`backtest-view.tsx` — `EffectiveRiskPanel` reads `result.run_config?.effective_risk`, source colors `T.primaryLight` for explicit, `T.text` for default, `T.text3` for inactive). Documents the full `app/lib/api/strategies.ts` type ladder (12 types/interfaces across phases 4/6/7) with a phase-of-introduction column. Documents the `apply-default-risk` proxy route added in Phase 6 and the unchanged PUT proxy. Documents the four non-obvious UX patterns and their research justification (NN/g default-acceptance ~70% rubber-stamp rate drove the anchor-counter ghost-text design; per-bot picker default = `Inherit new` because the strategy edit was the user's stated intent and snapshot is opt-in; `EffectiveRiskPanel` reads from snapshot rather than re-resolving so attribution stays stable after subsequent strategy edits; `StatusStrip` lives outside the graph because the original OutputPins coupled status display to canvas geometry that became untenable once the right-side risk-defaults pin landed). Cross-links to `psxDataPortal/docs/ARCHITECTURE.md` ADR-008 (the new decision record), `psxDataPortal/docs/EXITS_IMPLEMENTATION_PLAN.md` (phase plan), and `psxDataPortal/docs/research/EXITS_DECISION_SYNTHESIS.md` (research). The backend doc updates (BACKEND.md model row + ExitRules section, ARCHITECTURE.md ADR-008) live in the `psxDataPortal` repo's tracker entry. **Hybrid exits initiative complete end-to-end across 8 phases over 1 calendar day.** Two follow-ups remain explicitly out of scope: (a) Phase 4b parallel mirror exit-tree authoring on the right side of the editor canvas — deferred because the layout module's single-root anchor needs either a `mirror` flag or a coordinate post-processor; (b) `psx-ui` Jest/Vitest setup needed before the planned `InheritableField` 3-state component tests + submit-shape integration test can land.)

> Previous (2026-05-07) (**Exits Phase 7 — Backtest result Effective Risk panel landed.** The backtest result page at `app/app/backtest/backtest-view.tsx` now renders a dedicated read-only attribution panel for the four scalar risk guardrails the engine ran under (`stop_loss_pct`, `take_profit_pct`, `trailing_stop_pct`, `max_holding_days`), placed in the right column above the existing `deploy?` Ribbon so the run-summary context sits adjacent to the deploy decision. Each row displays label + value + a small uppercase source tag — `OVERRIDE` (primary tint, when the user typed a value into `/backtest/new`'s `<InheritableField>`), `DEFAULT` (text2, the run honored what the strategy authored at run time), or `INACTIVE` (text3, neither explicit nor default — guardrail off). Values formatted via the same `Number(v.toFixed(2))` trim used in the Phase 6 modal so 5 → "5%", 5.50 → "5.5%", and `max_holding_days` is integer-rounded with the "d" suffix. The panel reads from `result.run_config?.effective_risk` — the frozen snapshot the backend's `_resolve_run_config` (`backend/app/routers/strategies.py:62`) wrote via `EffectiveRisk.to_dict()` at run time, so re-rendering the same run never re-resolves against a strategy that may have changed since. Older results predating Phase 2 (no `run_config` or no `effective_risk` block) get a neutral muted "risk snapshot not recorded for this run" dashed-border placeholder rather than a guess — the UI never fabricates data that wasn't captured. **API surface** (`app/lib/api/strategies.ts`): added `EffectiveRiskSource` union (`"explicit" | "default" | "none"`), `EffectiveRiskResolution` (`{value: number | null, source: ...}`), `EffectiveRiskSnapshot` (record of the four field resolutions), and `BacktestRunConfig` interface (deliberately narrow — only `effective_risk` is typed since other run_config keys aren't rendered yet, and encoding shapes the UI doesn't read invites drift); extended `BacktestResultResponse` with optional `run_config`. **Verification**: `tsc --noEmit` clean; `npm run build` succeeds (36 routes, no warnings). Phase 8 (docs + ADR — BACKEND.md, FRONTEND.md, ARCHITECTURE.md, PROJECT_TRACKER.md, plus the EXITS_DECISION_SYNTHESIS ADR) is now unblocked.)

> Previous (2026-05-07) (**Exits Phase 6 — Per-bot inheritance picker modal landed.** When the strategy editor at `app/app/strategies/[id]/editor-view.tsx` saves a change to `exit_rules.default_risk` and the backend's PUT response carries an `inheritance_warnings` block (the Phase 3 contract — emitted iff at least one non-stopped bot was inheriting one of the changed fields), the editor now opens a per-bot picker modal instead of silently propagating. New component `app/components/strategy-editor/inheritance-warning-modal.tsx`: top diff strip renders each changed field as a pill in the form `Stop loss 5% → 8%` (old → new, primary tint on the new value); a "N inheriting · M snapshotting" running counter sits above the bot list; bulk "All inherit" / "All snapshot" toggles, then a per-bot row showing name + status (ACTIVE/PAUSED color-coded) + which fields the bot inherits, with a two-button radiogroup `Inherit new` / `Snapshot old`. Default for every bot is `Inherit new` — the strategy edit was the user's stated intent, so the safe default is to let it propagate; snapshot is opt-in per bot. **Wire path**: `performSave()` now snapshots `initialStrategy.exit_rules?.default_risk` BEFORE the PUT (the response carries the new value on the strategy and only a sliced view of old values inside `inheritance_warnings.old_values`, so we keep the full pre-save default for the apply call), then parses the JSON response, and when `inheritance_warnings.affected_bots.length > 0` opens the modal with `{warning, oldDefaultRisk, busy, error}` state instead of firing the "Draft saved" toast (the modal is the user's primary feedback). Cancel leaves the strategy edit committed but skips the propagate/snapshot decision — every affected bot keeps inheriting live, so the new default flows through on the next signal eval (same outcome as "all inherit", but explicit user opt-out). On confirm, `performApplyInheritance(decision)` POSTs `/api/strategies/{id}/apply-default-risk` with `{propagate_to_bot_ids, snapshot_bot_ids, changed_fields, old_default_risk}`; success closes the modal + flashes `Defaults applied · N inheriting · M snapshotted`; failure keeps the modal open (the user doesn't lose their per-bot picks) with the backend error rendered in an alert strip and busy state cleared so they can retry. **API surface** (`app/lib/api/strategies.ts`): added `RiskField` union type + `InheritanceWarningBot`, `InheritanceWarning`, `ApplyDefaultRiskRequest`, `ApplyDefaultRiskResponse` interfaces matching the backend Pydantic schemas; extended `StrategyUpdateResponse` with optional `inheritance_warnings`; added `applyDefaultRisk(jwt, id, body)` wrapper. **Proxy route** (`app/app/api/strategies/[id]/apply-default-risk/route.ts`): new POST handler — auths via NextAuth + signs backend JWT, parses body, forwards to `applyDefaultRisk`, bubbles ApiError as `{error, detail}` with the original status. PUT route was unchanged — `updateStrategy` already returns the full backend response (including `inheritance_warnings` when present), so it passes through verbatim once the type covered it. Suppressed for autosave (`silent: true`) so a background save never pops a modal under the user mid-keystroke. **Verification**: `tsc --noEmit` clean; `npm run build` succeeds (36 routes — `/api/strategies/[id]/apply-default-risk` registered, no warnings). **Phase 7 (backtest result Effective Risk panel) is the next unblocked piece**; Phase 8 (docs + ADR) gates on 7.)

> Previous (2026-05-07) (**Exits Phase 5 — Deploy/backtest InheritableField anchor-counter UX landed.** New `app/components/inheritable-field.tsx` is a generic 3-state wrapper for any field that can inherit a strategy-level scalar default: (1) **no default** → renders as plain editable input; (2) **inheriting/ghost** (default exists, form value `null`) → read-only input with the default rendered as ghost placeholder, "Inheriting · 5%" pill in primary tint, "Override" link (focusing the input also implicitly overrides — saves a click); (3) **overridden** (form value non-null) → editable input with "Overridden" pill in accent tint and "Reset to default" link that re-nulls the field and reverts to ghost. Anchor-counter principle (NN/g default-acceptance research): the default is **never** pre-filled, so override is always an active choice. **Wired into the bot create wizard** at `app/app/bots/new/page.tsx`: strategy fetch now captures `s.exit_rules?.default_risk`, passes it through `<UniverseAndRiskFields strategyDefaults={...} />`, and the wizard's `Preview` panel surfaces effective values via a small `eff()` resolver that mirrors the backend (`override ?? strategyDefault ?? null`) — inherited values render in `T.text3` with a "· inherited" suffix so users see the actual cap that'll apply. **Wired into the backtest run form** at `app/app/backtest/new/backtest-new-view.tsx`: new `useEffect` on `strategyId` fetches `/api/strategies/{id}` and sets `strategyDefaults` state; when the strategy authored a default for a field, the form's override is null'd out so the field starts in inherit/ghost (anchor-counter — no rubber-stamping). The four exit-risk fields (`stop_loss_pct`, `take_profit_pct`, `trailing_stop_pct`, `max_holding_days`) now render as `<InheritableField>`; `max_concurrent_positions` stays a plain `RiskField` since it's not part of `default_risk`. The `RunRail`'s `riskLabel` now folds inheritance: effective values are computed inline (override → strategy default → absent) with a `↳` prefix on inherited segments so the rail communicates both the cap and its source. **Universe-and-risk-fields component** at `app/components/universe-and-risk-fields.tsx` got a new optional `strategyDefaults?: DefaultRisk | null` prop; the four exit-risk inputs render as `<InheritableField>` when defaults are passed, fall back to plain `NumberInput` otherwise. **Submit shapes are unchanged at the wire level** — both pages already preserved `null` for blank fields, so inheritance falls through to the backend resolver (`backend/app/services/risk_inheritance.py`) at run time. **Verification**: `tsc --noEmit` clean; `npm run build` succeeds (35 routes, no warnings). **Deferred (separate task)**: psx-ui has no test runner configured (`npm run lint` is just `tsc --noEmit`), so the plan's "component-level tests for InheritableField (ghost / overridden / no-default states), submit-shape integration test" need a Jest/Vitest setup before they can land — flagged as a stand-alone follow-up rather than expanding Phase 5 scope. Phase 6 (per-bot inheritance picker modal) and Phase 7 (backtest result Effective Risk panel) are unblocked.)

> Previous (2026-05-07) (**Exits Phase 4 — Editor canvas: RiskDefaultsNode + StatusStrip + OutputPin removal landed.** The strategy editor at `app/app/strategies/[id]/editor-view.tsx` now authors the four scalar risk defaults (`stop_loss_pct`, `take_profit_pct`, `trailing_stop_pct`, `max_holding_days`) directly on the right side of the graph via a new pinned `RiskDefaultsNode`, replacing the three OutputPins (Backtest / Live signals / Automate) that previously fan'd out from the entry root. Single Bezier connector now runs from `rootOriginX/Y` to the RiskDefaultsNode pin at `(outputX, root.pinY)` — the node's top sits at `root.pinY - PIN_OFFSET_Y` so the pin lines up with the entry-root output. **State refactor**: `useState<ExitRules>` for `[exit]` is now writable and a new `[riskDefaults, setRiskDefaults]` keyed off `initialStrategy.exit_rules?.default_risk ?? {}` lives next to it; `handleRiskDefaultsChange` wraps `setRiskDefaults + setDirty(true)`; `buildUpdateBody(name, tree, exit, riskDefaults)` writes `exit_rules.default_risk` on every save. **Type updates** in `app/lib/api/strategies.ts`: new `DefaultRisk` interface added to `ExitRules`. **New components** under `app/components/strategy-editor/`: `risk-defaults-node.tsx` (220px pinned node, 4 numeric inputs in 2×2, empty input → null, integer-only on max-hold, 0–100 cap on percent fields matching backend Pydantic bounds, exposes static `WIDTH`/`PIN_OFFSET_Y`); `status-strip.tsx` (compact bottom-right pill carrying the navigation role the OutputPins used to — Backtest segment with gain/loss colored % + relative-time, Deploy segment with `StatusDot` pulse, optional Bot segment hidden until Phase 6 wires real binding state). The strip slides with the drawer using the same transition as the zoom-controls pill. The OutputPin component definition (~90 lines) was deleted since no callers remain. **Verification**: `tsc --noEmit` clean; `npm run build` succeeds (21 routes, 23.7s). **Deferred to Phase 4b**: parallel mirror exit-tree authoring on the right side. The plan's geometry rewrite ("ConditionTree rendering reused for the right-side exit tree") needs either a `mirror` flag in `layoutTree` or a coordinate post-processor for every node + wire — flagged as the riskiest piece in `docs/EXITS_IMPLEMENTATION_PLAN.md` with sandbox-first recommendation. Today the right-side authoring tree is not rendered; the `[exit, setExit]` state pair is wired so it can land without a second state refactor. Phase 5 (deploy/backtest InheritableField) and Phase 7 (backtest result effective-risk panel) are unblocked.)

> Previous (2026-05-07) (Backend: `assert_entry_rules_actionable` helper gates deploy/backtest/bot/status→ACTIVE on ≥1 leaf condition; schema now allows empty trees. Frontend: blank preset sends empty conditions so new strategies open with a clean canvas instead of a placeholder RSI rule)

## Project Summary
PSX Algos — marketing site + in-app experience for a no-code strategy authoring, backtesting, and paper-trading product for the Pakistan Stock Exchange. Next.js 16 App Router, inline-style React UI.

## Current Status
**Status**: Active — backend disassociation shipped (B044/B045/B046) and signal scanner universe wired up at deploy time (B047). Frontend wiring for those backend changes is now done end-to-end across all six slices.

## In Progress
- [ ] **Backend wiring — Phase 4: production cutover** — Vercel project, env vars (NEXTAUTH_SECRET must match Railway byte-for-byte), CORS origin add to backend, DNS, decommission old project. No frontend code changes here.

## Upcoming / Planned
- [ ] **Indicator documentation in the strategy editor.** Deferred from 2026-05-07 conversation. The indicator picker dropdown shows 34 standard TA names (Pivot, Support 1/2, Resistance 1/2, ADX, OBV, CMF, Williams %R, Stoch %K/%D, BB UPPER/MIDDLE/LOWER/%B, ATR/ATR %, Parabolic SAR, etc.) — labels are accurate but cryptic for users new to technical analysis. Aligns with the "learn → understand → earn" brand mission. Build a proper learning surface: (a) central `INDICATOR_INFO` catalog (full name + 1-line plain-English purpose + typical use case + reference link); (b) per-option sub-line in the Combobox dropdown showing the full name + short purpose; (c) deeper "Learn more" affordance — could be an inline tooltip, a side-panel, or dedicated `/learn/indicators/<name>` pages with example charts (real PSX data showing RSI oversold bounce, MACD crossover, BB squeeze breakout, etc.); (d) external references (Investopedia, original papers where relevant — Welles Wilder 1978 for RSI/ATR/ADX/Parabolic SAR, John Bollinger for BB, Gerald Appel for MACD); (e) decide whether each indicator needs its own visual chart preview in the picker (think TradingView's indicator hover-preview) or whether text + linked-out is enough. Scope to revisit when the wizard / editor onboarding flow gets dedicated UX time.
- [ ] Frontend session for the universe/risk picker + wizard slimming + deploy modal (see In Progress).

## Recently Completed
- [x] **fix(strategy-builder): preset chip concatenation — `50atr_percent` / `atrbb_percent_b` parse errors.** `PresetChipStrip` was calling `insertAtCaret(preset.expression)` (raw cursor insert), so clicking "ATR %" after typing `50` produced `50atr_percent` → `unexpected token 'atr_percent' at column 2`. Added `commitExpression()` that uses `tokenAtCaret` to replace the current identifier span, matching how autocomplete `commit()` works. Changed `PresetChipStrip` prop from `onInsert` to `onCommit`; `OperatorChipStrip` keeps `insertAtCaret`. `tsc --noEmit` clean. — (2026-05-18)
- [x] **SB10 — volatility-context preset chips (ATR %, Distance (ATR), Bollinger %B, BB Bandwidth) now one-tap insertable in `<ExpressionInput>`.** Frontend-only preset layer over SB1 + SB8 — **no backend changes** (all four expressions parse verbatim through `expression_parser.py`). Single-file diff at `app/components/strategy-editor/expression/expression-input.tsx` (+117 lines): `PRESET_CHIPS` module-scope catalog (four `PresetChip` records — `id: "atr-percent" | "distance-atr" | "bb-percent-b" | "bb-bandwidth"`, `label` short enough for a 44 px chip, `description` single-line tooltip + autocomplete hint, `expression` the inserted text, `matchKeys` lowercased autocomplete prefix-match keys); a new `<PresetChipStrip>` toolbar component rendered above the input on all viewports (desktop + mobile — wider chips than `<OperatorChipStrip>` since labels like "BB Bandwidth" have to fit; 44 px touch-target min-height; monospace font for the preview; `title` + `aria-label` carry the human-readable description for a11y; horizontal scroll with `WebkitOverflowScrolling: "touch"`); and an autocomplete insertion loop in the existing suggestion `useMemo` block that emits one suggestion per matching preset with `hint: "preset"`. Empty token surfaces all four; otherwise `matchKeys.some(k => k.startsWith(query))`. Keys containing operator chars (e.g. `%b`) cannot match mid-typing because `tokenAtCaret` only includes `[a-z0-9_]`, but the alphanumeric prefix alone (`atr`, `bandwidth`, `distance`) is sufficient by design. Two bare-identifier presets (`atr_percent`, `bb_percent_b`) — the backend TA service already publishes them. Two arithmetic compositions — `(close_price - sma_50) / atr` (Distance in ATR units; negative = below MA, positive = above) and `(bb_upper - bb_lower) / bb_middle` (BB bandwidth as a fraction of the midline; volatility-regime indicator). Insertion contract: chip `onClick` calls `insertAtCaret(preset.expression)` (same seam used by the autocomplete commit AND `<OperatorChipStrip>`) which inserts at the current selection range and restores the caret. The SB1 parser canonicalizes on save; `tryParseExpression` round-trips all four verbatim (no whitespace drift, no spurious parens). Save→reopen round-trip preserves user-authored expressions because `SingleCondition.value_source` stores the input text exactly (per the SB1 Sweep-2 contract); reparsing on reopen yields the same canonical AST. **Tester PASS**: `./node_modules/.bin/tsc --noEmit` clean (exit 0); `npm run lint` (= `tsc --noEmit` + `scripts/check-env.mjs`) clean; `npm run build` clean (Next 16.2.4 + Turbopack, `✓ Compiled successfully in 17.1s`, `Finished TypeScript in 9.9s`, all 23 static pages generated, no warnings — includes `/strategies/[id]` (the editor route that mounts `<ExpressionInput>`) and `/strategies/new`). Preset insertion smoke executed against the actual `tryParseExpression` / `expressionToSource` code path the chip onClick invokes downstream — all 4 chips on empty input pass exact-canonical round-trip with caret at `text.length`; mid-caret realistic scenario (`"2 * "` then tap `ATR %` → `"2 * atr_percent"`, caret 15) round-trips canonically; autocomplete with token `"atr"` returns `["ATR %", "Distance (ATR)"]`; with `"bandwidth"` returns `["BB Bandwidth"]`; with empty token returns all 4; save→reopen round-trip simulated for all 4 (`value_source` stored as `preset.expression` verbatim, reparsing yields the same string). Backend round-trip via `python -c "from app.services.strategy.expression_parser import parse_expression; ..."` for all 4 returned the expected `IndicatorRefNode` / `BinaryOpNode` shapes (no BE work needed). Playwright not exercised — editor route sits behind `proxy.ts` Google OAuth (matcher `/strategies/:path*`), no `playwright.config.*` or `playwright` dependency in `app/`; tester elected to exercise the contract via the production code path rather than introduce browser + auth fixtures for a single-file UI change. **Files**: 1 modified — `app/components/strategy-editor/expression/expression-input.tsx` (+117/-0). ADR-009 in `psxDataPortal/docs/ARCHITECTURE.md` amended with the SB10 preset-layer note. SB10 dossier at `psxDataPortal/docs/design_call_dossiers/SB10_volatility_presets_2026-05-15.md`. **Validates the ADR-009 thesis**: SB10 is the first non-backend SB capability to ship purely on the SB1+SB8 surface — "SB3/SB8/SB10/SB11 ship as pure indicator/UI work" is now empirically true for SB8 and SB10. — (2026-05-15)
- [x] **SB8 frontend mirror — math helpers `abs`, `max`, `min`, `round`, `log` now authorable in `<ExpressionInput>`.** Companion to the SB8 backend sweep on `psxDataPortal` (shipped same day, single coordinated commit pair). `app/lib/api/strategies.ts` adds `MathFnName = "abs" | "max" | "min" | "round" | "log"` and `FunctionCallNode` (`type: "function_call"`, `name`, `args: ExprNode[]`) to the `ExprNode` union. `app/lib/strategy/expression.ts` adds the exported `MATH_FN_NAMES` whitelist set + private `MATH_FN_ARITY` map mirroring backend `_MATH_FN_ARITY`; `parseIndicatorRef` renamed to `parseIdentPrimary` with the math-fn-precedence branch (`MATH_FN_NAMES.has(name) && peek().kind === "LPAREN"` → `parseFunctionCallTail`); render / nodePrec / walkDepth / walkAndCheck / hasIndicatorRef each gain a `function_call` case matching the backend exactly. `app/components/strategy-editor/expression/expression-input.tsx` autocomplete iterates `MATH_FN_NAMES` and surfaces `abs( … )` / `max( … )` / `min( … )` / `round( … )` / `log( … )` snippets when the current token is empty or prefix-matches a fn name. Validation surfaces stable reason codes shared with backend (`math_fn_arity`, `round_decimals_invalid`, `unknown_math_fn`) so the inline error caret renders the same message in the browser that the API would emit on save. Strict NaN propagation matches backend / Pine `math.*` semantics (any `None` arg → whole call `None` → outer condition `False`). **Tester PASS**: `npx tsc --noEmit` clean, `npm run lint` clean, `npm run build` clean (Next 16.2.4 + Turbopack, 17.5 s compile, full route table); no new Playwright config (5 fns wired statically and exercised by FE build). **Files**: 3 modified — `app/lib/api/strategies.ts` (+13/-1), `app/lib/strategy/expression.ts` (+114/-3), `app/components/strategy-editor/expression/expression-input.tsx` (+14/-0). Dossier: `psxDataPortal/docs/design_call_dossiers/SB8_math_helpers_2026-05-15.md`. ADR-009 in `psxDataPortal/docs/ARCHITECTURE.md` amended with the SB8 extension note. **Unlocks SB10 / SB11 on the editor side**: users can now type `abs(close_price - sma_20) / atr_14 < 0.5` directly. — (2026-05-15)
- [x] **`/strategies/new` "blank" preset now creates an actually-blank strategy.** User: *"when we create a new strategy why does it auto populate with RSI(14) condition, shouldn't it be blank on new?"*. Two-repo fix because the previous behavior was a backend constraint that the frontend was working around with a placeholder rule. **Backend (`PSX-Algos-Backend`)**: `EntryRules._validate_tree` in `app/schemas/strategy.py` was enforcing both depth ≤32 (structural) AND ≥1 leaf condition (business rule) at the schema layer, blocking the wizard from ever creating a truly empty strategy. Refactored: schema-level validation now only enforces depth (the structural concern); the leaf-required rule moved to a new `assert_entry_rules_actionable(entry_rules)` helper that accepts both Pydantic instances and persisted dicts. Helper invoked at every execution-path entrypoint: `update_strategy` when transitioning to ACTIVE/PAUSED (`routers/strategies.py`), `run_backtest` (`routers/strategies.py`), `deploy_strategy` (`routers/strategy_signals.py`), `TradingBotService.create_bot` and `start_bot` (`services/trading_bot.py` — the latter re-checks at start time in case the bound strategy was edited down to empty after bot creation). All four convert ValueError → HTTPException(400) with a clear "add a condition before deploying / backtesting / binding to a bot" message. DRAFT and ARCHIVED status are open — drafts are by definition incomplete, archived is a soft tombstone. Tests: existing `test_all_empty_groups_rejected_on_entry` flipped semantics to `test_all_empty_groups_allowed_on_entry_schema` (now asserts the schema accepts) plus two new tests for the helper itself (rejects empty, accepts with leaf, both via Pydantic and dict). Four new router tests in `test_strategies_crud.py` (create empty DRAFT works → 201, activate empty → 400 with leaf message, add-condition-then-activate works in one PUT, backtest empty → 400) and one new in `test_strategy_signals.py` (deploy empty → 400). All 10 new tests pass; the 2 pre-existing failures in `test_strategy_signals.py` (`test_deploy_strategy_with_symbols`, `test_redeploy_overwrites_universe` — confirmed pre-existing via `git stash` rerun) are unrelated. **Frontend (`PSXAlgos-Frontend`)**: `presetToEntryRules('blank')` in `app/strategies/new/page.tsx` now sends `conditions: []` instead of the apologetic `RSI < 50` placeholder. Editor's empty-tree path was already wired (`isTreeEmpty = tree.children.length === 0` at `editor-view.tsx:1708` plus the "Add first input" affordance at `:1889`), so opening a freshly-created blank strategy now shows a clean canvas with the picker ready to receive the user's first condition. `BACKEND.md` updated to document the new schema-vs-actionability split. `npm run lint` (tsc --noEmit + check-env) clean. — (2026-05-07)
- [x] **Combobox — unfiltered browse no longer caps at 8.** User followup after the previous Combobox fix: *"still we get like 8 options, are these all we have?"* with screenshot showing dropdown listing Close/Open/High/Low/Volume/SMA 20/SMA 50 only. Backend `/strategies/meta/indicators` returns 34 indicators across 8 groups (5 price + 5 moving_averages + 3 macd + 5 momentum + 6 volatility + 2 trend + 3 volume_indicators + 5 support_resistance), but the post-refactor `Combobox` was still doing `options.slice(0, maxResults)` even when not filtering — and `maxResults` defaults to 8. The cap made sense for filtered search ("show top 8 typing matches") but not for browsing the full menu (which has its own `max-height: 240, overflowY: auto` scrollable container ready to handle a long list). Fix: when `query` is empty, return `options` unsliced; the cap only applies during active typing. Updated the JSDoc on `maxResults` to clarify it's a typing-time cap. Portfolio's symbol picker (~824 active PSX tickers) renders all 824 list items now when browsing — modern React + simple divs handles this comfortably (<16ms paint), and users will narrow it by typing within a keystroke or two anyway. If perf ever becomes an issue we can drop in `react-window` virtualization without changing the API. — (2026-05-07)
- [x] **Strategy editor — indicator picker now lists every indicator, not just the currently-selected one.** User: *"why do we only have RSI indicator to select from where's everything else? on the nodes in graph strategy builder"*. Backend `/strategies/meta/indicators` (`backend/app/routers/strategies.py:1057-1066`) was correctly returning all 8 groups (price, moving_averages, macd, momentum, volatility, trend, volume_indicators, support_resistance), but the shared `Combobox` component (`components/atoms.tsx:952`) was conflating two pieces of state: `value` was both *the committed selection* and *the filter query*. So when the drawer opened with `value="RSI (14)"`, the dropdown filtered options by "rsi (14)" — collapsing the menu down to the one indicator that already matched. Refactored `Combobox` to follow the WAI-ARIA combobox authoring pattern that Headless UI / cmdk / react-select all use: added internal `query: string | null` state separate from the parent-controlled `value`, where `null` means "not actively filtering, show every option." `displayValue = query ?? value` so the input renders the committed value when closed, and the typed query while editing. `openDropdown()` resets `query=null` so opening always shows the full list; commit/Escape/outside-click reset it back. `onChange` still fires on every keystroke so portfolio's "submit-as-typed" behavior is preserved (inspected both Combobox callers in `portfolio-view.tsx:1241` symbol picker and `:1313` strategy picker — both rely on `value` mirroring typed text on submit). Added `inputRef` + `requestAnimationFrame(() => inputRef.current?.select())` on focus so the next keystroke replaces the committed value instead of appending to it (Safari needs the rAF). Added a chevron toggle button on the right of the input that flips between rotate(90deg) closed and rotate(-90deg) open via `transform: rotate` + 120ms ease, so the open-the-menu affordance is visible — no need to know that focusing the input opens the dropdown. Chevron has `tabIndex={-1}` and `e.preventDefault()` on mousedown so it doesn't steal focus from the input. Added `role="combobox"`, `aria-expanded`, `aria-autocomplete="list"` to the input to follow the ARIA Combobox APG. ArrowDown when closed now opens via `openDropdown()` (was just incrementing highlight). The four `<Combobox>` callers (indicator LHS at `editor-view.tsx:3012`, indicator RHS at `:3238`, portfolio symbol at `portfolio-view.tsx:1241`, portfolio strategy at `:1313`) work without any caller-side changes — public API unchanged. `npm run lint` (tsc --noEmit + check-env) clean; `npm run build` clean (all 30 routes regenerate). — (2026-05-07)
- [x] **`/strategies/new` Step 2 — name + description are now editable.** User: *"the name, description stuff is not editable"* on the new-strategy wizard. Step 2 was rendering `preset.defaultName` as a static `<div>` (with a fake blinking cursor `|` decoration) and the description as a static `<div>` showing either a hardcoded mean-reversion blurb or `${preset.desc}. Universe and risk are picked when you deploy or backtest.` — neither was wired to state, and `buildCreateBody` only sent the preset defaults to the API. Replaced with a controlled `<input maxLength={50}>` styled to preserve the 36px head-font + `borderBottom: 2px solid T.primary` underline (transparent bg, native caret tinted with `T.primary`), and a `<textarea rows={3}>` styled like the previous div panel (surfaceLow bg, faint outline, vertical resize). Added `name` + `description` `useState` in `WizardPage` initialized from `preset.defaultName` / `${preset.desc}.`, with a `useEffect` keyed on `preset.key` that resets both whenever the user picks a different starting preset in Step 1 (so going Back → swap preset → Continue starts clean instead of carrying stale text). New `slugify(input)` helper produces the live ID preview shown under the name field (lowercase, non-alphanumeric → `_`, trim, max 40, falls back to `untitled`); the slug is preview-only — backend assigns the canonical `strategy_id` on create. Right-rail summary `<DotRow label="Name">` now reflects the typed value (falls back to `preset.defaultName` if user blanks the field). `buildCreateBody(preset, name, description)` trims both, falls back to preset defaults when empty, and sends user-typed values to `POST /api/strategies`. `npm run lint` (tsc --noEmit + check-env) clean. — (2026-05-07)
- [x] **Segmented control — pill colour fitted to parchment theme.** User: *"white universe selected field doesn't go with the theme"*. The `.psx-segmented-pill` rule in `app/app/globals.css` was using `var(--surface-2)` which resolves to pure `#ffffff` on light theme — too cold against the cream `#f7f3ea` track. Switched to `var(--surface-bright)` (`#fffcf3`), the warm cream-white that the rest of the parchment palette uses for "lifted" surfaces. Affects every `.psx-segmented-track` instance, currently only the universe-mode picker on `/backtest/new`. Dark theme also benefits — `--surface-bright` is `#322c20` (subtle warm-dark lift) vs `--surface-2`'s `#15120c` which had less contrast. — (2026-05-02)
- [x] **Strategy picker — drop the show/hide drafts toggle, always show all strategies.** User: *"why do we need show/hide drafts in strategy just show all of them"*. Removed `useSessionStorage<boolean>("psx:bt:show-drafts", false)`, the `if (s.status === "DRAFT" && !showDrafts) return false` filter line, the `draftCount` variable, and the bottom-of-popover "show/hide drafts (N)" button JSX from `StrategySection` in `app/app/backtest/new/backtest-new-view.tsx`. ACTIVE/PAUSED/DRAFT/ARCHIVED still sort in that order so non-drafts surface first. `useSessionStorage` import stays — `UniverseSection` uses it for `psx:bt:filters-open`. `npm run lint` (tsc --noEmit) + `npm run build` clean — (2026-05-02)
- [x] **Frontend — `/backtest/new` UX redesign.** User: *"I don't like this page from UX point of view, it's just shit, do research and improve it"* + chose option A (full two-column with live summary rail) and asked for *"online and thorough research for relevant frameworks and how can we make it interactive and interesting for users"*. Three parallel research agents fanned out: (a) algo platforms — TradingView Strategy Tester, QuantConnect Cloud Optimization, Composer.trade, VectorBT/Backtrader, pyfolio/QuantStats, Robinhood/IBKR/eToro; (b) form interaction patterns — Stripe Checkout, Linear filter UI, Figma properties panel, Notion filters, Vercel Web Interface Guidelines, Emil Kowalski animation rules, Rauno Freiberg interaction notes, NN/G EAS framework, WCAG 2.2 SC 2.4.11; (c) repo audit of existing primitives. Synthesis: take Composer's right-rail-preview colocation, QuantConnect's live computed-scope idea, pyfolio's tearsheet-header typography for the rail, Linear's cmdk-style multi-select popover, Stripe's form+summary two-column. Reject motion library (vanilla CSS reaches all needed interactions; @starting-style + View Transitions + grid-template-rows + FLIP cover everything; 30+KB Motion bundle unjustifiable for a config form). **Built four new primitives**: `components/disclosure.tsx` (chevron-led collapsible with `grid-template-rows: 0fr → 1fr` cross-browser height animation, 240ms `cubic-bezier(0.77,0,0.175,1)`); `components/segmented.tsx` (3-mode segmented control with absolutely-positioned active pill that translates between segments via `transform`, no layout shift); `components/multi-select-popover.tsx` (cmdk-style popover combobox with search + checkbox list + selected chips above trigger, items NOT reordered on selection per Linear pattern — research said reorder disorients); `components/use-session-storage.ts` (SSR-safe sessionStorage hook for UI scratch state like which sub-disclosures are open). Added 5 CSS rules to `globals.css`: `psx-cell-flash` keyframe (180ms diff highlight), `.psx-disclosure-body[data-open]` selector, `.psx-segmented-track`/`.psx-segmented-pill` slide, `.psx-press:active{transform:scale(0.97)}` button momentum, `.psx-sticky-rail` (max-height: 100dvh, sticky top: 16px), `.psx-mobile-cta-bar` (sticky bottom + safe-area inset). All wrapped by the existing `@media (prefers-reduced-motion: reduce)` global suppressor. **Rewrote `app/app/backtest/new/backtest-new-view.tsx`** end-to-end (~1500 lines). New shape: editorial header → CSS-grid two-column (`1.9fr 1fr` desktop, `1fr` mobile, `gap: 56px`) with form on the left and `<RunRail>` (sticky `aside`) on the right. Form sections: `<StrategySection>` (replaces 14-pill wall — popover combobox with search input, ACTIVE/PAUSED pinned, drafts collapsed behind a "show drafts (N)" toggle persisted to sessionStorage); `<RangeSection>` (kept preset chips + react-day-picker custom popover, but default flipped from 1M to 1Y because 1M is too short for a meaningful backtest and the rail's "252 trading days × 824 tickers = 207,648 candidate decisions" readout makes the cost of longer ranges visible); `<CapitalSection>` (Rs prefix overlay + thousands separator on blur via `Intl.NumberFormat("en-PK")`, focus-state-driven so commas don't fight the caret mid-edit + presets unchanged); `<UniverseSection>` (segmented control swaps between `All active · 824 tickers` / `By sector` / `Specific tickers` — cross-fade body via View Transitions API where supported with 180ms opacity-keyframe fallback; sector mode renders `<MultiSelectPopover>`; ticker mode keeps a hand-rolled SymbolPickerInline; numeric filters nested in a `<Disclosure>` with summary chip "no filters" / "N filters applied"); `<RiskSection>` (load-bearing per research — kept fully visible, NOT hidden behind Advanced; `Balanced / Aggressive / No caps` preset row drives the 5 inputs in one click, each input pairs with a plain-English caption like "Exit if a trade loses more than this"). **`<RunRail>`** is a `position: sticky` aside styled like a pyfolio tearsheet header — small-caps mono kickers + tabular-num values + italic-serif strategy name; rows are `scope` (range + trading days), `capital` (Rs formatted), `universe` (mode + computed size), `risk caps` (concise comma-list); each row gets a `· default` tag in muted mono if it matches `RISK_DEFAULTS` / 1Y / 1M PKR / All-active, so users can SEE which choices they haven't deliberately made (Tognazzini's "make defaults visible" via NN/G EAS); a derived "estimated scope: ~N candidate decisions = M trading days × K tickers" panel appears once both numbers are live (QuantConnect-style live cost preview, repurposed for scope); big primary `Run backtest` CTA pinned to the bottom of the rail; mobile collapses to a sticky-bottom CTA bar with `scroll-padding-bottom: 96px` to satisfy WCAG 2.2 SC 2.4.11 ("Focus Not Obscured"). **Universe mode and risk-cap defaults** are now expressed via three explicit constants (`RISK_DEFAULTS = stop 5% + max 5 pos`, `RISK_AGGRESSIVE = stop 8% + take 20% + max 8`, `RISK_NONE = all null`) and the form initializes to `RISK_DEFAULTS` (load-bearing constraints visible) instead of all-null. **Backend payload semantics preserved** — `handleRun` translates the segmented mode into the existing `BacktestRequest` shape: "all" → `stock_filters: null + stock_symbols: null` (or filters-only when numeric filters added); "sector" → `{ sectors: [...], ...numeric }`; "ticker" → `stock_symbols: [...]`. So the server contract from B045 is unchanged. Verified visually via a temporary `/dev-preview/backtest-new` route that bypassed auth (mounted `BacktestNewView` with stub strategies, mock auth-free; `getAllStocks()` ran live so the rail computed `824 tickers × 252 trading days = 207,648` against real PSX data). Screenshots captured for desktop (1440×900) and mobile (390×844); strategy popover + sector popover + segmented mode-switch all working; route deleted post-verification. `npx tsc --noEmit` + `npm run lint` + `npm run build` clean — all 30 routes regenerate. Research artifacts saved to `/tmp/research-algo-platforms.md`, `/tmp/research-form-patterns.md`, `/tmp/audit-psx-ui-primitives.md` for future redesigns of `/bots/new` and the deploy modal which share the same `UniverseAndRiskFields` problem (deferred — out of scope for this session) — (2026-05-02)
- [x] **Prod incident — `/strategies` and `/bots` were 500ing on `psx-algos.vercel.app`; root-cause was migration drift on Neon, not FE code.** User screenshot of "This page hit a snag · digest 2608416988" on `/strategies`, confirmed `/bots` same. Probe trace: every endpoint that touched the `strategies` or `trading_bots` table returned 500; every endpoint that didn't (`/health`, `/subscriptions/plans`, `/strategies/meta/indicators`) returned 200. SQLAlchemy was raising `UndefinedColumn` on `Strategy.scan_filters` (B047) and `TradingBot.stock_filters` (B044) because the prod Neon DB was at revision `043` while the deployed backend code was at `047`. The deployed `backend/railway.json` had `startCommand: "uvicorn app.main:app ..."` with no `alembic upgrade head` step, so B044-B047 migrations never ran on Neon. **Fix landed in three commits on the backend (`Simba256/psx-trading-view`):** (1) `cbfc53d` chained `alembic upgrade head &&` in front of uvicorn — this got Railway to *try* migrating on each deploy. (2) `cc25ef9` added a temporary `bash -c 'set -ex; alembic current; alembic heads; alembic upgrade head; exec uvicorn ...'` diagnostic chain to surface alembic state in deploy logs after observing that the chain ran but the schema stayed at 043. Diagnostic showed alembic was connecting to the localhost placeholder (`postgresql://postgres:postgres@localhost:5432/psx_portal`) because Railway only had `DATABASE_URL` set, not `DATABASE_SYNC_URL`, so `settings.database_sync_url` resolved to its hardcoded default — alembic dutifully connected to nothing, exited 0 against an empty migration plan, the chain advanced, and uvicorn booted while Neon stayed unmigrated. (3) `3207665` rolled the diagnostic back to the clean `&&` form and added `alembic/env.py::_resolve_sync_url` + `_async_url_to_sync` helpers: when `DATABASE_SYNC_URL` is unset or the localhost placeholder, derive the sync URL from `DATABASE_URL` by stripping `+asyncpg` from the scheme and rewriting `?ssl=require` → `?sslmode=require` (asyncpg vs psycopg query-param convention). Other query params (Neon's `options=...`, branch flags, etc.) pass through verbatim. Net: Railway's standard `DATABASE_URL` binding is enough — operators don't need to know about the asyncpg/psycopg split. Validated locally with both env vars set (alembic → 047 head ✓) and with only `DATABASE_URL` set (alembic → 047 head ✓). **The actual schema fix** was `alembic upgrade head` run from a local shell against the prod Neon URL (B046's destructive 5-column drop accepted per the original B046 commit's stated trade-off). After the migration applied, prod re-probes flipped from 500 → 200 on `/strategies`, `/strategies/runs`, `/bots`, and `/bots/{nonexistent_id}` correctly returns 404 instead of 500. `BACKEND.md` Configuration table flagged `DATABASE_SYNC_URL` as optional with the derivation note; `.env.production.example` commented out the line; production deployment section documented the new `alembic upgrade head &&` chain. — (2026-05-02)
- [x] **Frontend — Slices 5 & 6: strategy now carries only the rules, end-to-end.** Slice 5 collapses `/strategies/new` from a 3-step wizard to a 2-step wizard: Step 1 (preset picker) → Step 2 (name + description + after-create destination). The old Step 2 (universe radio buttons + hardcoded risk-defaults block + universe summary) is gone — universe and risk are now picked at deploy / backtest / bot time, not strategy creation. `buildCreateBody` slimmed to `{ name, description, entry_rules, exit_rules: {} }`; `UNIVERSES` array, `Universe` interface, `universeToFilters` helper, hardcoded `position_sizing` + `max_positions: 5` payload all dropped. The final-summary panel was rebuilt around base rule + confirmation + status with a "universe and risk picked at deploy / backtest / bot time" callout so users know where those decisions live now. Step indicator + meta + tip-text re-flowed to "Step X of 2 · ~1 minute". Slice 6 strips the strategy editor of every drawer/canvas affordance for the dropped concerns: deleted `ExecutionDrawer` (sizing slider + 4 exit-toggle rows + warning chip), `ExitRow` row component, `ExitKey` type, `parsePct`/`parseDays`/`fmtPct`/`fmtDays` helpers, `ExecNode` canvas card, the `"execution"` `SelKind` branch, and the universe label that used to render after the strategy name in the editor header. The canvas now wires the entry-tree root directly to the three output pins (Backtest / Live signals / Bot) — replaced the `ROOT_TO_EXEC_GAP + EXEC_TO_OUTPUT_GAP` pair with a single `ROOT_TO_OUTPUT_GAP = 402` and reflowed the three output curves around `outputCurveCtrlX = midpoint(rootOriginX, outputX)`. `buildUpdateBody` lost the `sizing` parameter and the `position_sizing` field; `exit` state held in the editor is now read-only (the signal-tree round-trips on save but no surface authors it yet — out of scope for this slice). Cleaned up cascading types in `lib/api/strategies.ts`: dropped `PositionSizing`, `PositionSizingType`, `RiskManagement`, the four scalar guardrails on `ExitRules`, and the `position_sizing_types` field on `IndicatorMeta`; slimmed `StrategyResponse` to mirror backend `StrategyBase` (no more `position_sizing` / `risk_management` / `stock_filters` / `stock_symbols` / `max_positions`); slimmed `StrategyCreateBody` to `{ name, description, entry_rules, exit_rules }`. `EMPTY_META` in the editor page lost `position_sizing_types`. The `/strategies` list view's `universe` column went away with it: `deriveUniverse` helper deleted, `Strategy.universe` field dropped, the `{ label: "universe", width: "110px" }` column removed, all eight `renderCell` indices reflowed (3-7 → 3-7 minus one). `tsc --noEmit` + `npm run lint` + `npm run build` all clean — every static-prerender survives, all 30 routes regenerate. — (2026-05-02)
- [x] **Backend — B047: Signal scanner universe wired (deploy-time, on the strategy).** User: *"so strategy deployment should have the universe not itself, so they can deploy it with different universes as well"* and then *"or maybe we can have it stored with strategies as well … they can just pick the universe and universe can include both KSE-100 and banking as well if they wanted so they technically can just use that one deployment for it"*. Decision: single deploy-time universe stored on the strategy itself (not a new table) with a `scan_` prefix to distinguish from the bot universe — the StockFilters shape already supports composing multiple sectors / symbol sets in one filter, so multi-universe-at-once is unnecessary. Migration `047_strategy_scan_universe.py` adds two nullable JSON columns to `strategies` (`scan_filters`, `scan_symbols`); idempotent ADD via inspector. `Strategy` model gains the columns. New `DeployRequest` Pydantic schema; `DeploymentStatus` + `DeployResponse` expose `scan_filters` / `scan_symbols`. `POST /strategy-signals/strategies/{id}/deploy` now accepts an optional body and validates `stock_filters` against `StockFilters` (the same schema bots/backtests use). Re-deploying with a body overwrites the universe; body-less deploy lands NULL/NULL → scanner produces zero signals until re-deploy. `POST .../undeploy` clears both fields so a future re-deploy starts clean. `signal_scanner._get_stock_universe` no longer stubbed: 1) `scan_symbols` allowlist short-circuits, 2) `scan_filters` runs `_symbols_matching_filters` (mirrored from `trading_bot_engine` — `Stock.is_active=True + sector_name IN (...)`), 3) both NULL → empty list. Dropped the deferred-warning flag. **Tests:** 6 router tests added (deploy without body, deploy with symbols, deploy with filters, redeploy overwrites, undeploy clears, GET deployment exposes universe) and 4 scanner unit tests in new `test_signal_scanner_universe.py` (symbols short-circuit, sector filter resolution, both NULL → empty, no-sectors filter falls through to all active). Full router/task/service suite: 888 passed, 6 pre-existing failures unrelated to B047. **`BACKEND.md` updated** with the new Strategy model row + deploy/undeploy endpoint signatures — (2026-05-02)
- [x] **Backend — universe + risk disassociated from Strategy (B044 / B045 / B046).** User: *"now let's disassociate universe from strategy, like universe should be selected at the time of creating paper trading bot, or at the time of running backtest, and I feel like it should not be associated with a strategy from the beginning"*; clarified to also move guardrails + sizing + max concurrent (the entire Stage 2 of the wizard, not just universe) since *"things like guardrails (stop loss, take profit, position size, etc) should also part of backtesting and not strategy creation"*. Three Alembic revisions, three commits, each leaving the backend in a working state. **B044** (commit `c6a38dc`, migration `044_bot_universe_and_risk.py`): added 8 columns to `trading_bots` — `stock_filters` (JSON), `stock_symbols` (JSON), `stop_loss_pct` / `take_profit_pct` / `trailing_stop_pct` (Numeric(10,4)), `max_holding_days` (Integer), `position_sizing` (JSON), `risk_management` (JSON). All nullable. `BotCreate` / `BotUpdate` / `BotResponse` schemas gained matching fields. `TradingBotService.create_bot` / `update_bot` persist them. `TradingBotEngine` got new resolver helpers (`_get_stock_universe(bot, strategy)`, `_resolve_exit_rules(bot, strategy)`, `_resolve_position_sizing(bot, strategy)`) that prefer the bot row with the strategy as B044/B045 transitional fallback. New tests `test_create_bot_with_universe_and_risk`, `test_update_bot_universe_and_risk`, `test_create_bot_without_universe_falls_back_to_strategy`. **B045** (commit `6f18c4b`, migration `045_backtest_run_config.py`): added `backtest_results.run_config` JSON column (snapshot of universe + risk parameters this run executed under, so re-opening an old result shows what it ran with). `BacktestRequest` gained 9 optional fields (universe + risk + sizing + max_positions). `BacktestResultResponse` exposes `run_config`. New router helper `_resolve_run_config(data, strategy)` merges request over strategy (B045 transitional) and returns the canonical dict; both async and sync paths persist the snapshot. Engine signature unchanged — overrides are overlaid into `strategy_dict` at the router. New unit tests `test_resolve_run_config.py` (6 cases): no overrides falls back to strategy (B045 behavior), then post-B046 NULL-passthrough; universe/risk/sizing override; legacy guardrail keys scrubbed from strategy.exit_rules. **B046** (this commit, migration `046_drop_strategy_universe_and_risk.py`): destructively dropped 5 columns from `strategies` (`stock_filters`, `stock_symbols`, `position_sizing`, `risk_management`, `max_positions`). Strategy model + `StrategyBase` / `StrategyCreate` / `StrategyUpdate` / `StrategyResponse` slimmed to just `name`, `description`, `entry_rules`, `exit_rules`, lifecycle. `ExitRules` schema dropped the four scalar guardrails (`stop_loss_pct` / `take_profit_pct` / `trailing_stop_pct` / `max_holding_days`) — only `conditions` remains; legacy keys in stored JSON are silently ignored on read. `_resolve_run_config` rewritten: no strategy fallback, NULL-passthrough for the four nullable slots, scrubs legacy guardrail keys from `strategy.exit_rules` before merging request scalars. `trading_bot_engine` resolvers stripped of strategy fallback paths (bot is the only source). Strategy-router CRUD lost universe/risk handling. `signal_scanner._get_stock_universe` stubbed to `return []` with a one-shot `logger.warning` (deferred decision per user — see In Progress). Affected test fixtures (`bot_strategy`, `sig_strategy`, `exp_strategy`, `user_a_strategy`, `other_strat`, `VALID_STRATEGY` payload) updated to construct strategies without the dropped fields. **Test summary**: 87/88 in the targeted suite (only pre-existing failure `test_today_signals_requires_pro` is unrelated to B1-B3 — verified against `main` pre-changes). **Per-user decisions**: data lost intentionally (no strategy → bot/backtest data migration), three separate migrations not one, signal scanner deferred for now, frontend left untouched (next session). `BACKEND.md` updated with new Strategy / TradingBot / BacktestResult model rows describing the migration set — (2026-05-02)
- [x] **Backtest — split run-config onto `/backtest/new`; results page is now display-only.** User: *"on Backtest results page we shouldn't have date range, instead there should be a different page for running backtests… on /backtest page there can be a button to run new backtest and the flow could be you select the strategy, the domain (stocks), the date range there and then run backtest, and from individual backtest results page re-run backtest can also take you to that same page"*. After confirmation, agreed scope: yes to `/backtest/new`, keep universe strategy-bound (filters/symbols stay on the strategy editor), redirect to results on completion. New `app/app/backtest/new/page.tsx` server component fetches the user's strategies (page_size 100) + parses optional `strategy_id`/`start`/`end` query params (re-run prefill). New `app/app/backtest/new/backtest-new-view.tsx` client view: chip-based StrategyPicker (one button per strategy with status tint — DRAFT/ACTIVE/PAUSED/ARCHIVED), the same preset-chips + react-day-picker DateRangeBlock extracted from the results page (sticky-Custom behavior preserved), a UniverseNote panel that links to the strategy editor (because filters/symbols live there), and a single Run button. Run flow mirrors the old `handleRerun` — POST `/api/strategies/[id]/backtest` → poll → on completion `router.push('/backtest?strategy_id=N&backtest_id=M')` so the result lands on the dedicated display page. Empty state when user has no strategies → CTA to Build a strategy. `backtest-view.tsx` slimmed to results-only: removed `DayPicker`/`parseISO`/`useRouter`/`useRef` imports + `BacktestJobPending`/`BacktestJobStatus` types, deleted `isoLocal`/`todayIso`/`pollJob`/`handleRerun`/`autoRun` effect, dropped `startDate`/`endDate`/`running`/`result` (now read-only from `initialResult`) state, removed `<DateRangeRow>` render and the entire `DateRangeRow`/`DateTrigger`/`presetRange`/`detectActivePreset`/`formatPickerLabel`/`PresetKey` block at the bottom (~210 lines gone). Re-run button is now a `<Link href="/backtest/new?strategy_id=N&start=...&end=...">` that prefills the saved result's window — clicking it lands on the run page with the same range, user can tweak or fire as-is. `app/app/backtest/page.tsx` no longer reads `?run=1` (autoRun is dead). Repointed: editor-view.tsx Run backtest pill → `/backtest/new?strategy_id=N` (was `/backtest?strategy_id=N&run=1`); strategies/new/page.tsx after-create "backtest" branch → `/backtest/new?strategy_id=N` (was results page, which would have been empty); `/backtest` index header gains a `Run new backtest` primary CTA (only when `hasStrategies`); index empty-state CTA when `hasStrategies=true` now points to `/backtest/new` instead of `/strategies`. Updated `components/frame.tsx` doc comment to describe the new topology. `npm run build` clean — `/backtest/new` registered in the route manifest alongside `/backtest`. `tsc --noEmit` clean — (2026-05-02)
- [x] **Backtest — `/backtest` now lists individual *runs*, not strategies.** User: *"we should list backtest runs on /backtest page instead of strategies which you could get on strategy page, like different time window with same strategy should be selected as different run"*. Strategy-level discovery moved entirely to `/strategies` (where each row already shows latest backtest summary). New backend endpoint `GET /strategies/runs` (in `backend/app/routers/strategies.py`, registered BEFORE `/{strategy_id}` so FastAPI's int-parser doesn't steal `runs`) JOINs `BacktestResult ⨝ Strategy` and returns user-scoped run history newest-first with `strategy_name` baked in. New schemas `BacktestRunRow` + `BacktestRunListResponse`. Three regression tests: cross-strategy aggregation + ordering by `created_at`; other users' runs filtered out (no leak); `?limit=N` cap respected; unauth → 401. Frontend wrapper `listAllBacktestRuns(jwt, limit)` + `BacktestRunRow` type added to `lib/api/strategies.ts`. `app/backtest/page.tsx` no-params branch now `Promise.all`s the runs fetch with a 1-row strategies probe (so the empty-state can branch on "no strategies" vs "have strategies but no runs"). `backtest-index-view.tsx` rewritten end-to-end: column set is `strategy | window | total return | sharpe | max DD | trades | ran | →`, sort options are `most recent / total return / sharpe / trades / strategy`, click → `?strategy_id=N&backtest_id=M` (deep-links to that run via the existing `getBacktestResult` hydration in `page.tsx:107-118`). Window column renders compact "Mar 1 → Jun 30, '24" same-year / "Mar 1 '24 → Jun 30 '25" cross-year. Empty state branches: hasStrategies=false → "build a strategy" CTA, hasStrategies=true → "pick a strategy and hit Run backtest" pointing at /strategies. Backend tests 19/19 + 36/36 passing, `tsc --noEmit` clean. Updated `backend/BACKEND.md` with the new endpoint row — (2026-05-02)
- [x] **Backtest page — full audit, 7 issues fixed across UI + metrics.** User asked *"check the backend source of each and every value on this page and check they should be accurate and relevant, they shouldn't be wrongly calculated or being fetched from a wrong backend or hardcoded"*. Traced every rendered value (header range, bars, capital, ran-ago, total return, Sharpe, Max DD, win rate, profit factor, avg hold, equity legend, KSE-100 ticker) from pixel back to backend. Eight values were correct + data-driven; seven issues fixed: (1) `formatDateLabel` dropped the year so a 1-year backtest collapsed to "May 01 → May 01" — added `formatRangeHeader` that always shows year on the right, and on both sides when start/end span years. (2) `formatRan` capped at minutes (819m ago) — now escalates to h/d/mo, matching the index page's `formatRelative`. (3) Hardcoded `KSE-100 +0.42%` chip in the top header (`components/frame.tsx`) — wired to live `GET /market/indices?period=1D` via new public proxy `app/api/market/indices/route.ts` (60s edge cache). New `useKseTicker` hook + `formatPct` helper. Loading/failure → chip is hidden (no fabricated number); color flips to loss tint when down; refreshes every 5 min. Both desktop and mobile drawer chips use the same hook. (4) Equity-curve legend advertised both "strategy" and "KSE-100" lines but `<EquityCurve>` was only ever called with the strategy series — dropped the KSE-100 legend item. (5) Sharpe / Sortino risk-free-rate units bug in `metrics_calculator.py` — annualized return was multiplied by 100 to put it in percent before subtracting `RISK_FREE_RATE`, but `RISK_FREE_RATE = 0.05` (a fraction) wasn't scaled, so a 5% rate behaved like 0.05%. Net: ratios overstated by ~5/vol pp. Fix scales `RISK_FREE_RATE` to percent at the subtraction site for both ratios; added two regression tests (returns whose annualized value equals exactly the rf — Sharpe/Sortino should sit near zero, not in the thousands). (6) Loss classification in `metrics_calculator.py` was `pnl <= 0` so a zero-PnL trade was counted as a loss — changed to strict `<` so a breakeven trade is neither a win nor a loss; added a regression test. (7) Frontend rendered `profit_factor=null` (no losing trades) as "0.00" because `num()` coerces null → 0 — now distinguishes the undefined case and renders "∞" with sub-label "no losing trades". Pushes: `dac1939` (UI fixes #1/#2/#4), `0ddc8a9` (Sharpe RFR backend), `8cec453` (KSE wiring), `c588711` (breakeven backend), `6fb1573` (profit factor frontend). Backend metrics tests: 36/36 passing. `tsc --noEmit` clean — (2026-05-02)
- [x] **Backtest — date range now defaults to 1M instead of inheriting the saved result's window.** User feedback: *"why is custom date range selected by default when the page opens, it should be 1M by default"*. Strategy 10 had a saved backtest with arbitrary start/end dates, and the previous init `initialResult?.start_date ?? yearAgoIso()` lazy-seeded the inputs from those dates — which almost never match a preset's exact `(today − N, today)` window, so `detectActivePreset()` correctly fell into Custom. Switched the init to `presetRange("1M").start / .end` unconditionally, so every page open primes the inputs for a fresh 1M run regardless of any prior result. The user doesn't lose the saved-run context — `EditorialHeader.meta` still renders `dateRange = formatDateLabel(result.start_date) → formatDateLabel(result.end_date)` from the loaded result. Removed the now-dead `yearAgoIso()` helper. `tsc --noEmit` + `check-env.mjs` clean — (2026-05-02)
- [x] **Backtest — date range is now preset chips + react-day-picker (no more white native popup).** User follow-up: *"when I click on the date, it opens a white calender to selct can we change it are there other options or align it better with theme?"* `<input type="date">`'s native popup is rendered by the browser's UI layer — `colorScheme` toggles its chrome but you can't paint it to match a custom palette, so the white overlay was an unfixable native limitation in Paper mode. Replaced with a presets-first interaction that fits the editorial aesthetic and only falls back to a calendar grid when the user explicitly opts in. Three pieces in `app/app/backtest/backtest-view.tsx::DateRangeRow`: (1) Preset chips row — `1M / 3M / 6M / YTD / 1Y / 3Y / All / Custom` rendered as small uppercase mono chips (`fontSize: 11`, `padding: "4px 9px"`, `T.outlineFaint` border idle, `T.surface3` fill + `T.outlineVariant` border active). Click a non-Custom chip and `presetRange(key)` derives `{start, end}` (end = today, start = today minus the offset; YTD = Jan 1 of current year; All = today − 10y) and writes to the lifted `startDate/endDate` state. (2) `detectActivePreset(start, end)` reads back which chip should be active — endDate must equal today and start must equal one of the preset start dates; otherwise the row falls into Custom mode (so opening a saved backtest with arbitrary dates lights the Custom chip without a user click). A `customSticky` flag tracks explicit Custom picks so the chip stays lit even if the user happens to type dates that match a preset. (3) When Custom is active, two `<DateTrigger>` buttons render under the chips. Each is a dashed-bottom-rule button showing the current date in `Mon D, YYYY` form; click opens an absolutely-positioned popover anchored under the trigger containing a `<DayPicker mode="single">` from `react-day-picker@9.14`. Click-outside (mousedown listener on `document`) and `Escape` close the popover; selection writes back through `onChange` and auto-closes. Cross-bounds enforced via `disabled` matchers (from-trigger: `{after: parseISO(endDate)}`; to-trigger: `{before: parseISO(startDate), after: parseISO(today)}`). Theming: new `app/components/calendar.css` overrides the `react-day-picker/style.css` rules using the existing `globals.css` CSS variables (`var(--text)`, `var(--surface-low)`, `var(--primary)`, `var(--outline-variant)`, etc.). Because globals.css already swaps these vars under `[data-theme="dark"]`, the picker auto-themes for Paper and Amber from a single rule set — no JS branching. Selected day fills with `--primary`, today's day shows `--primary` text + bold, hover gets `--surface-3`, weekday headers use mono uppercase + `--text-3`, nav buttons get a subtle 1px border that lights on hover, popover container uses `T.surfaceLow` + `T.outlineVariant` border + soft shadow + `psx-pop-in` 140ms entrance animation. Removed: native `<input type="date">` element + `colorScheme: mode` workaround + `useTheme` import (only `useT()` needed now). New deps: `react-day-picker@9.14.0`, `date-fns@4.1.0` (peer). Lint + `tsc --noEmit` clean — (2026-05-02)
- [x] **Backtest — date-range inputs now feel native to the editorial theme.** User screenshot (Paper mode): *"the date selector fields seem a bit off, like they don't belong with the theme of the page"*. Two issues in `backtest-view.tsx::DateRangeRow`: (1) `colorScheme: "dark" as const` was hardcoded, forcing the browser's native date-picker chrome (calendar pop, spin chevrons) into dark UI even on Paper — fighting the page palette. (2) The input itself used a hard `T.surface3` fill + `1px solid T.outline` border + 3px radius, reading as a boxed form-field instead of an editorial inline value. Fix: imported `useTheme` to get `mode`, set `colorScheme: mode` so the picker UI follows the active theme, and rebuilt the input as a barely-there bottom-rule field (transparent fill, no top/side border, `1px solid T.outlineVariant` bottom rule, `outline: none`, `borderRadius: 0`, `padding: "4px 4px 3px"`). Reads as part of the row text instead of a separate widget — same fontMono / fontSize / textColor as the kicker label and arrow. `tsc --noEmit` clean — (2026-05-02)
- [x] **Backtest — auto-run flash now matches the actual button label.** User screenshot (URL `/backtest?strategy_id=10`): *"when I click on run backtest I come on this page, it says to click on run backtest but there isn't one and is this hardcoded data?"*. Investigation showed the button IS there — top-right of the EditorialHeader at `backtest-view.tsx:409` — but its label flips dynamically: `{running ? "Running…" : result ? "Re-run" : "Run backtest"}`. Strategy 10 had a stored prior result so the label was "Re-run", while the Option-B strip-only autoRun effect (line 285) hard-coded its flash text to *"Set a date range and click Run backtest."* — telling the user to click a button whose label was "Re-run". One-line fix: branched the flash on `initialResult` so it surfaces *"Set a date range and click Re-run."* when prior data exists and *"Set a date range and click Run backtest."* otherwise; added `initialResult` to the effect's deps. Confirmed displayed metrics (+37.8%, 1.20 Sharpe, 2.3% MaxDD, 41% win rate, 3.87 PF, 10d avg hold, equity curve) are real, not hardcoded — they come from `page.tsx:117` `getBacktestResult(jwt, sid, latest.id)` (the latest stored backtest for the strategy); empty case would render `—` in every Lede tile. The hardcoded sectors panel was already removed (see `backtest-view.tsx:629-636`). `tsc --noEmit` clean — (2026-05-02)
- [x] **Strategy editor — floating action bar horizontal padding bumped.** User screenshot showed Run backtest / Pause / + Bot pressed against the rounded pill edges. Container had `padding: 6` (uniform) on a `borderRadius: 999` pill, so the curved corners visually clipped the first/last button. Bumped to `padding: "6px 16px"` — vertical stays compact, horizontal gets breathing room on both ends. Buttons untouched (already `size="sm"` — shrinking further would hurt tap targets) — (2026-05-02)
- [x] **Strategy editor — Validate button removed (decorative, always-passing).** User asked *"what is validate button for? it always gives valid, what does it check?"* — investigation showed the floating-action-bar Validate button (`editor-view.tsx:1971`) wired to `handleValidate` which just called `leafCount(tree)` and unconditionally flashed `"Strategy is valid · N conditions connected"`. No real checks: empty tree → still "valid", incoherent indicator/operator pairs → still "valid", no backend dry-run, no plan-tier gate. User chose remove over implementing real validation. Five edits in `app/app/strategies/[id]/editor-view.tsx`: dropped `leafCount` from the `lib/strategy/tree` import (still exported there because `hasAnyLeaf` calls it internally), deleted `handleValidate`, dropped the `onValidate={handleValidate}` prop on the `<Canvas>` call, dropped `onValidate` from Canvas's destructured params and its TS prop type, and removed the `<Btn>Validate</Btn>` + the 1px divider that followed it from Canvas's floating action bar. Run backtest / Deploy / + Bot are the only buttons left in that bar — all of which exercise the strategy through the backend, which performs real validation. `tsc --noEmit` clean — (2026-05-02)
- [x] **Backtest — user-picked date range required (no more hidden defaults).** User asked *"do we select time range for backtesting, like from here to here backtest it"* — surfaced that `handleRerun` was hard-coding `start = today − 1yr`, `end = today` with no UI for it. Followed up with *"can we do it so that when backtesting you need to set dates"* and picked Option B (drop auto-run, require explicit click) over Option A (keep auto-run with editable defaults), since "need" is the operative word. Three pieces in `app/app/backtest/backtest-view.tsx`: (1) New `startDate` / `endDate` component state, lazy-initialised from `initialResult?.start_date / .end_date` so re-runs default to the displayed result's range and net-new strategies get `today − 1yr → today`. Local-time `isoLocal()` helper — not `toISOString().slice(0,10)` — so PKT users don't see UTC drift on the inputs. (2) `handleRerun` now reads from state and validates inline before firing: empty inputs → "Pick a from / to date range", `start >= end` → "From date must be before to date", `end > today` → "To date cannot be in the future", span < 7 days → "Date range must span at least 7 days". (3) The auto-run effect that fired on `?run=1` arrival is replaced with a strip-only version: it removes the `run` query param (so refresh doesn't re-prompt) and surfaces "Set a date range and click Run backtest." as a flash. Editor's "Run backtest" link still navigates to `/backtest?strategy_id=N&run=1` — but that now opens the page with focused inputs instead of auto-firing. New `<DateRangeRow>` component pinned below `<EditorialHeader>` (above the metrics scroll area, with `borderBottom: outlineFaint` so it visually attaches to the header): two `<input type="date">` controls with `min/max` cross-bounds (`from.max = endDate || today`, `to.min = startDate, to.max = today`) so the native pickers self-clamp and `colorScheme: "dark"` so the calendar picker follows the dark theme. Header CTA dynamically labelled — `result ? "Re-run" : "Run backtest"`. Empty-state copy updated to *"no backtest yet — pick a date range above and click Run backtest"*. `tsc --noEmit` + `check-env.mjs` clean — (2026-05-02)
- [x] **Strategy editor — info tooltips ("i in circle") on condition drawer fields.** User feedback (with screenshot of the condition drawer): *"in the conditions menu many people might be new and not know what each option means or represent, we can have in i in circle thing that I have seen on many places for information and you hover over it so it show you a message/description about it."* Added a generic, reusable affordance rather than scattering bespoke help text. Three pieces: (1) `components/icons.tsx` gains a 12px `info` glyph (circle outline + dot/stem). (2) `components/atoms.tsx` gains an `<InfoTooltip text>` component — 14px hit target, hover/focus reveals a 240px-wide popover positioned `top: 100% + 6px, left: 0` with `T.surface3` background, `T.outlineVariant` border, `T.text2` body text in `T.fontSans` 11.5px. Native `title` attribute set as touch fallback, `tabIndex={0}` for keyboard focus, `pointerEvents: none` on the popover so it doesn't trap the cursor. (3) `<Kicker>` and `<Combobox>` both gain optional `info?: string` props that forward to `InfoTooltip`; the Combobox's internal `<Kicker>` already wraps its label so wiring there meant the indicator field gets the icon for free. Wired in `editor-view.tsx::ConditionDrawer`: new `FIELD_INFO` constants (indicator / period / operator / comparedTo) attached to all four section labels via the new `info` prop. Each `OPERATOR_OPTIONS` row also gained a `hint` field with a plain-English description; the operator buttons now pass that as `title` so hovering `×↑` reveals "Crosses above — the indicator just rose past the value (was below on the previous bar, now at or above)" instead of leaving the user to guess the symbol. The Constant/Indicator pills under "compared to" got similar `title` hints. The reference-indicator Combobox (rendered in compare=Indicator mode) also gets an `info` describing the right-hand-side semantics. No layout shift — the icon sits inline with the kicker's existing `display: flex; gap: 8` so it slots naturally beside the underline+label. `lhsLabel` and `period` semantics unchanged. Build green (15/15 pages), `tsc --noEmit` + `check-env.mjs` clean — (2026-04-30)
- [x] **Strategies — manual rename rejects (409) on duplicate name with clear toast.** Companion to the create-side autosuffix. User wanted a *different* behavior on rename: *"I would say b is more intuitive, so the user knows what the problem is instead of guessing"* — i.e., when the user explicitly types a name in the header that collides, fail loudly rather than silently appending " (1)" to what they typed (which would feel like the app fighting them). Two changes: (1) `app/api/strategies/[id]/route.ts` PUT now fetches the user's strategies and rejects the request with `409 {"error": "A strategy named \"X\" already exists — pick a different name."}` when `body.name` matches another strategy of theirs (excluding the one being edited so trivial saves don't self-conflict). Best-effort: list-call failure falls through and lets the backend save (no false-positive blocking). (2) `editor-view.tsx::performSave` error path used to dump raw response text into the toast — for JSON envelopes from the proxy that surfaced ugly `{"error":"..."}` text. Now parses the response as JSON, extracts the `error` string, and falls back to raw text if not JSON. So the user sees "A strategy named "Foo" already exists — pick a different name." in the flash instead of `{"error":"..."}`. Autosave still fires on every edit; if the colliding name remains, each retry shows the same error which is a feature — the problem stays visible until the user fixes the name. Build green (15/15 pages), `tsc --noEmit` clean — (2026-04-30)
- [x] **Strategies — filename-style "(N)" suffix on duplicate names at create time.** User report (with screenshot showing 6× "Untitled strategy" + 3× "RSI Bounce v1"): *"it keeps creating strategies with same name, like there should be a check by default it suggests Untitled Strategy but in case if that name already exists then it should be like filenames like they put (1) ahead of it and if that too exists then (2) then (3) and so on"*. Wizard hardcodes `name: preset.defaultName` (Step 3 displays the name as static text, not an `<input>`), so the blank preset always submits "Untitled strategy" and the backend has no uniqueness check. Fix lives in the proxy `/api/strategies` POST (`route.ts`) so it's the choke point for every create path: before calling `createStrategy`, fetch the user's existing strategies (`getStrategies` page_size=1000) and pass the proposed name through a new `nextAvailableName(desired, taken)` helper that strips an existing trailing ` (N)` (so re-creating from "Foo (1)" jumps to "Foo (2)", not "Foo (1) (1)" — matches macOS Finder / Google Drive) and iterates "(1)" "(2)" … until it finds one not in the set. Best-effort: if the list call fails we fall through with the original name (duplicates are valid in the schema, just confusing in the UI). Cap of 10,000 iterations with a `Date.now()` fallback for the pathological case. PUT path (rename) intentionally untouched — manual renames may be deliberate versioning. Build green (15/15 pages), `tsc --noEmit` clean — (2026-04-30)
- [x] **Strategy editor — graph edits now autosave (no more lost work on refresh).** User report: *"If I change the graph, add nodes, or conditions, when I refresh, it reverts back to the initial state."* Root cause was UX, not wiring: every edit set `dirty=true` and the `Save draft` PUT pipeline (handleSaveDraft → performSave → `/api/strategies/[id]` PUT) was correct, but persistence was gated on the user clicking the button. Modern editor expectation is auto-save, so the user reasonably refreshed without clicking and lost the in-memory tree. Fix in `editor-view.tsx`: (1) `performSave` gains a `silent?: boolean` param so autosave doesn't toast on every keystroke burst; only the "needs at least one condition" guard and the success "Draft saved" / empty-group warning are gated on `!opts.silent`. Error flashes still surface unconditionally. (2) New `useEffect` keyed on `[dirty, saveStatus, confirmModal, groupModal, tree, name, exit, sizing]` schedules a `setTimeout(performSave({silent:true}), 800)`; cleanup cancels — that's the debounce. Skipped while `saving` (no concurrent PUTs) or while a confirm/group modal is open (modals capture intent; we don't write around them). (3) Belt-and-suspenders `beforeunload` listener active when `dirty || saveStatus === "saving"` so accidental refresh inside the 800ms debounce or during the in-flight PUT triggers the browser's "leave site?" prompt. The header's existing "saving… / saved Xs ago / unsaved changes" indicator now reflects autosave state without code change. The explicit `Save draft` button + dependent-bot impact modal stay — autosave bypasses the modal (it's an explicit-save guard for bound strategies; routine drafting shouldn't be gated on it). Build green (15/15 pages), `tsc --noEmit` clean — (2026-04-30)
- [x] **Strategy editor — newly inserted empty group opens the InsertPicker, not the GroupDrawer.** User feedback (with screenshot): *"when we add a new group it opens the group tab instead it should show option to select condition or add another group option here"*. The Phase D `handleAddGroup` was doing two things on insert: (1) `setSelection({kind:"group", id: fresh.id})` to open the GroupDrawer, AND (2) `setPendingPicker({parentId: fresh.id, index: 0})` to auto-pop the new group's first-child picker. Both fired but the drawer rendered on top of the picker, so the user saw the AND/OR/Delete/Ungroup drawer instead of the condition/sub-group choice the new empty group actually needed. The drawer's purpose is editing logic / ungrouping after the fact — not first-population — so dropping the auto-selection is the right call. One-line fix in `editor-view.tsx::handleAddGroup`: removed `setSelection({...})`. The `pendingPicker` arming stays, so the new empty group's add-slot now auto-opens its picker on first paint and the user immediately gets the *"+ Add first input"* affordance with the Condition / Empty group ▸ choice. Drawer is still reachable post-creation by clicking the gate or the GroupBox border. Build green (15/15 pages), `tsc --noEmit` clean — (2026-04-29)
- [x] **Strategy editor — gate box enlarged so AND/OR glyph fits inside.** User feedback (with screenshot): *"the box for the gate is small compared to the text maybe we can make it bigger so that text comes inside the box instead of spilling out"*. The 68×68 gate frame paired with `<GateGlyph size={GATE_W}>` (font-size = 68) was overflowing horizontally — italic "AND" at fs=68 renders ~100px wide, clearly past the box. Two-line fix: bumped `GATE_W` 68 → 104 and `GATE_H` 68 → 76 in `lib/strategy/layout.ts`, and changed the glyph call in `editor-view.tsx::GateButton` to `<GateGlyph logic={logic} size={GATE_W * 0.5}>` so the font scales to half the box width (52px) instead of equaling it. Italic "AND" at fs=52 ≈ 78px wide × 50px tall now sits comfortably inside a 104×76 frame with ~13px horizontal padding and ~13px vertical. Cascading effects all benign: `ROOT_GATE_X=410` is still anchored (the `columnLeftX` formula compensates for `GATE_W` so root gate's left edge stays at 410), `gateLeftX` stays right-aligned within each level's column, the slot Y formula `gateY + GATE_H/2 + ADD_SLOT_GAP + ADD_SLOT_H/2` shifts down 4px following the taller box, the bounding-box gate inclusion widens consistently, and `execAnchorX = root.gateX + GATE_W` shifts the output column right by 36px (acceptable per the user's prior *"why do we even need to cap"* mandate). Leaf-to-gate horizontal travel shrinks from 212→160px since `COLUMN_PITCH` is fixed and `GATE_W` is now larger — wires still have a clean Bezier curve, slightly more compact. README constants line updated with a one-sentence note on the `GATE_W * 0.5` glyph sizing. Build green (15/15 pages), `tsc --noEmit` clean — (2026-04-29)
- [x] **Strategy editor — add-slot positioned just below the gate glyph.** User feedback (with screenshot): *"the add condition buttons they keep going down, can we make it so that they are just below the AND or OR text for the gate"*. Initial layered-layout implementation positioned each group's add-slot at `childrenBottom + ADD_SLOT_GAP + ADD_SLOT_H/2` — visually correct for short groups, but for tall populated groups (n=2+ leaves with subtrees) the slot drifted to the bottom of the children band, far from the AND/OR gate glyph, breaking the "add input to *this* gate" visual association. One-line fix in `layout.ts::place()` for both empty and populated branches: `slotCy = gateY + GATE_H/2 + ADD_SLOT_GAP + ADD_SLOT_H/2`. Now the slot sits 12px below the gate's bottom edge, regardless of children span. For tall groups this lands the slot mid-tree (between the children that flank the gate vertically), but it stays inside the gate's column band so it never horizontally overlaps a sibling subtree. The `subtreeHeight` reservation (`childrenSpan + ADD_SLOT_GAP + ADD_SLOT_H`) is unchanged — generous enough to keep the slot inside the group's vertical band even when the gate's actual midpoint shifts on unbalanced subtrees, so siblings still don't collide. README invariants section updated to call out the new positioning. Build green (15/15 pages, 12.6s), `tsc --noEmit` clean — (2026-04-29)
- [x] **Strategy editor — layered (logic-graph) canvas layout.** User feedback: *"I don't like the fact that all the nodes are supposed to be at the same x-level (coordinate) a general logic gate diagram or graph is not like that I think"*. Pre-rewrite, `layout.ts` placed every leaf at `frameX + GROUP_PAD` regardless of nesting depth, so nested-group leaves sat in essentially the same vertical column as the parent's leaves (only an 18px indent per level). Visually a stack of boxes-within-boxes, not a graph. Full rewrite of `lib/strategy/layout.ts` to a layered (Sugiyama-style) model: every node gets `level = depth from root` (root=0, root.children=1, …) and X is derived purely from level via `columnLeftX(L) = ROOT_GATE_X - NODE_W + GATE_W - L * COLUMN_PITCH` (`COLUMN_PITCH = NODE_W + COLUMN_GAP = 280`). Root gate at fixed `ROOT_GATE_X=410`; deeper levels are progressively to the LEFT, so a depth-3 leaf lands at column index 3 (~560px left of root). Y is assigned top-down by recursive packing using `subtreeHeight(node)` (leaves = `NODE_H`, groups = `Σ child heights + GAP*(n-1) + ADD_SLOT_GAP + ADD_SLOT_H`), so siblings stack without overlap and slot-reservation prevents the previous overlap regression. Per the user's preference *"add input to this gate rather than per-column"*, the Phase D between/end/empty slot variants are collapsed into a single per-group add-slot positioned at the bottom of the children band, horizontally centered on the gate column — semantically "add an input to this gate", and `slot.isEmpty` flips the copy to "Add first input" (root-empty also gets the *"Click + to add your first condition or group"* hint). `<GroupBox>` now wraps each group's true bounding box (computed as min/max over all descendants + own gate + add-slot, then padded by `GROUP_PAD` and `GROUP_LABEL_H`) instead of a column-collapsed rectangle, matching the user's *"Keep groupbox"* choice. ExecNode + OutputPins now anchor to `root.gateX + GATE_W` regardless of `showGate` (vs. `rootOriginX` which collapses for single-child trees) so the output column stays at a stable canvas X for all root configurations. Wire pass unchanged conceptually — each leaf wires to its parent gate (now a much longer horizontal travel since columns are spread out), each populated sub-group wires to its parent gate. `resetView` now actually fits to content (`layoutBounds` + ExecNode/OutputPin extension), and a one-shot `useEffect` on canvas mount auto-fits so users opening existing complex strategies don't land on a mostly-empty viewport. Imports in `editor-view.tsx` updated: removed `EMPTY_SLOT_W/H`, `END_SLOT_H`, `SLOT_SIZE`; added `ADD_SLOT_H`, `layoutBounds`. `InsertSlot` rewritten as a single-variant always-visible button (no proximity-reveal, no touch detection). `lib/strategy/README.md` rewritten with the new `./layout.ts` section + invariants. Build green (15/15 pages, 13.5s), `tsc --noEmit` clean — (2026-04-29)
- [x] **Strategy editor — dynamic output column (ExecNode + Backtest/Live signals/Automate pins).** User report (with screenshot): *"if we add multiple layers and groups of nodes, the graph goes beyond the end nodes, the output nodes, are they always fixed at a position?"* Yes — `ExecNode` was hardcoded at `x=620` and the three `OutputPin`s at `x=880` (pre-Phase-C anchors), while the entry tree's root gate slides right with depth (`gateX = maxChildRight + ROOT_HGAP`, +148px per nested populated group via `INNER_HGAP + GATE_W`). Result: at depth ≥ 2, `root.gateX` exceeded 620 and the gate→exec wire routed backwards across the canvas. Fix replaces all four hardcoded X literals (and three control-point literals on the exec→output wires) with values derived from `rootOriginX`: `execX = rootOriginX + 142` (`ROOT_TO_EXEC_GAP=142` preserves the depth-1 multi-child pixel-stable visual at `execX=620`); `outputX = execX + 260` (`EXEC_TO_OUTPUT_GAP=260` matches the old 880-620 spacing); `execPinRightX = execX + 236` (replaces the literal `860` — matches ExecNode's right Pin offset); `outputCurveCtrlX = outputX - 10` (replaces the literal `870` control point). `rootOriginX/Y` computation moved up so it's declared before exec/output derivation; the original duplicate at the wire block is now a back-reference comment. All three `<OutputPin>` calls and the `<ExecNode>` call now read `x={execX}` / `x={outputX}` instead of hardcoded numbers; all four `<Connector d=...>` strings were rewritten. The output column slides right naturally as tree complexity grows — pan/zoom (already wired) lets the user navigate. Single-child depth-1 trees do see exec move ~240px left (no gate to anchor against, so `rootOriginX` collapses to the leaf pin); user explicitly rejected the floor-cap option ("why do we even need to cap"). Build green (15/15 pages, 23.5s), `tsc --noEmit` clean — (2026-04-29)
- [x] **Strategy editor — end-slot overlap fix.** User report (with screenshot): *"the add condition blocks are overlapping do we have a solution for that"*. Regression introduced by the always-visible end-slot rebuild — `collectSlots` was emitting the 140×32 button at `last.y + last.h + GAP + END_SLOT_H/2` (44px below the last child's bottom edge), but `layout.ts::size()` was still computing each populated group's bounding box as `Σ children.h + GAP·(n−1)` with no reservation for that button. Result: the slot poked 26px below its parent group's box, and the parent group's own end-slot landed on top of nested-group end-slots. Fix is one line in `size()`'s `childCount > 0` branch — `sumH += END_SLOT_OFFSET + END_SLOT_H` — so every populated group's height includes its own end-slot. This cascades correctly: `place()` already advances siblings via `cy += childSz.h + GAP`, so populated-group siblings now leave 44px more breathing room; `<GroupBox>` reads `group.h` straight, so the dashed box visually wraps the "+ Add condition" button (the slot is now logically inside its parent group's authoring zone, which matches the semantic — clicking it inserts into *this* group). `layoutBounds`'s recursive `visitForEndSlot` is now redundant for nested groups but kept for the root (root returns `{w: innerW, h: innerH}` without `GROUP_PAD`/`LABEL`, and root.y/h aren't part of the bound walk anyway). `lib/strategy/README.md` extended with the layout-invariant note. Build green (15/15 pages, 15.8s), `tsc --noEmit` clean — (2026-04-29)
- [x] **Strategy editor — end-slot discoverability fix.** User report: with a single RSI condition on the canvas, no obvious way to add a second — the Phase D end-slot was a 16px round pip at opacity 0 (proximity-revealed within 80px), and a depth-1 tree has no group-box to hover-target. Fix splits the inline `+` slot model into three discrete behaviors instead of two: `between` slots stay small (16px) and proximity-revealed (necessary for clutter control in groups with many siblings), `empty` stays as the wide root-empty placeholder, and `end` is now a wide always-visible button (140×32, dashed-border, "+ Add condition" copy). New layout constants `END_SLOT_W=140, END_SLOT_H=32, END_SLOT_OFFSET=GAP=12`; `collectSlots` emits `end` slots at `cy = last.y + last.h + END_SLOT_OFFSET + END_SLOT_H/2` so the button sits one full sibling-gap below the last child rather than overlapping its bottom edge. `layoutBounds` extended with a recursive end-slot pass so the world layer / SVG sizing accounts for the always-visible button at every group depth. `InsertSlot::opacity` proximity logic narrowed from `slot.variant !== "empty"` to `slot.variant === "between"` — `end` and `empty` both stay at full opacity. New `end` render path renders the button as a dashed-border affordance (`T.surface2 + "aa"` background, hover/picker-open tightens border to `T.outlineVariant` and bumps bg to solid `T.surface2`); `InsertPicker` `topOffset={END_SLOT_H + 6}` instead of 22 so the popover anchors below the taller button. Build green (15/15 pages), `tsc --noEmit` clean. `lib/strategy/README.md` updated with new constants + slot-variant docs reflecting the always-visible end behavior — (2026-04-28)
- [x] **Condition tree — Phase E: group editing (logic toggle, ungroup, cascade delete).** Closes out the 5-phase tree work. New helpers in `lib/strategy/tree.ts`: `ungroupAt(root, id)` (immutable, splices a group's children up into the parent at the same index, root is a no-op since root *is* the strategy) and `countEmptyGroups(root)` (counts non-root groups with `children.length === 0` for the save-time soft warning). `editor-view.tsx::GroupDrawer` rebuilt — header now shows `root group | group`, the AND/OR logic toggle (carried over from Phase C), a children summary line *"3 children — 2 conditions, 1 sub-group"*, and an action footer with **Delete** (variant=danger, left-aligned), **Ungroup** (variant=outline), **Close** (variant=ghost). Both Delete and Ungroup are `disabled` with explanatory `title` tooltips when the selected group is the root. Three new helpers `describeCondition`, `describeNode`, `describeRoot` build plain-English expressions for the BEFORE/AFTER modal: parens render only when a sub-group's logic differs from its parent's, so the modal shows exactly the semantic shift the ungroup would cause (e.g. BEFORE `RSI > 50 OR (Volume > avg AND Close > MA50)` → AFTER `RSI > 50 OR Volume > avg OR Close > MA50` because parent OR now glues the inner AND's children). `<UngroupConfirmModal>` fires only when `target.logic !== parent.logic` (matching logics ungroup immediately — no semantic change); BEFORE block neutral background, AFTER block has a `T.warning` border tint, Cancel is autofocused as a primary-styled button and Ungroup is right-positioned as `outline` so muscle-memory `Enter` is non-destructive (Gap 21). `<DeleteGroupConfirmModal>` fires only when the group has 2+ children (single-child / empty groups skip the modal). New state `groupModal: {kind: "ungroup" | "delete-group"; groupId} | null` drives both. Handlers: `handleUngroupGroup(id)` does the logic-match check then either calls `performUngroup` or arms the modal; `handleDeleteGroup(id)` does the 2+ child check then either calls `performDeleteGroup` or arms the modal. `performDeleteGroup` runs the same `hasAnyLeaf(removeNode(t, id))` floor the leaf delete uses, so a cascade delete that would empty the strategy is blocked with the standard *"A strategy needs at least one condition"* flash. Phase E2: new `useEffect` global keydown listener fires `handleDeleteGroup(selection.id)` when `Delete`/`Backspace` is pressed with a group selected (skipped while any modal is open or focus is in `INPUT`/`TEXTAREA`/`isContentEditable`). Phase E3: `performSave` now appends an empty-group hint to the success flash when `countEmptyGroups(tree) > 0` — *"Draft saved · 1 empty group will always evaluate to true"* — non-blocking since the backend treats `[]` as `True`. `lib/strategy/README.md` updated with `ungroupAt` and `countEmptyGroups` rows. `STRATEGY_TREE_PLAN.md` E4 acceptance ticked except for the user-verifiable smoke scenario. Build green (15/15 pages), `tsc --noEmit` + `check-env.mjs` clean. Smoke scenarios deferred to user verification (Google-OAuth-gated): build a depth-3 tree → ungroup the inner group whose logic differs → modal shows BEFORE/AFTER → confirm → save → reload → tree shape matches. Delete an inner group with 3 children → confirmation modal → confirm → descendants gone — (2026-04-27)
- [x] **Condition tree — Phase D: inline `+` insertion (corner pill retired; tree authored on-graph).** New layout export `collectSlots(root): InsertionSlot[]` walks every group (including the root) and emits one slot per insertion point: `between` slots vertically centered in each adjacent-children gap, an `end` slot just below the last child, or a single wider `empty` placeholder centered where a group's first child would land. New constants `SLOT_SIZE=16`, `EMPTY_SLOT_W=140`, `EMPTY_SLOT_H=108`. Editor side: new `<InsertSlot>` component renders the `+` in canvas world coords with a proximity-reveal opacity model — pointer viewports fade in within 80px (world) of the cursor, touch viewports (`(hover: none) and (pointer: coarse)`, detected via new `useTouchPointer()` hook) keep `+` slots fully visible. The `empty` variant is always at full opacity regardless of pointer; root-empty additionally renders the copy *"Click + to add your first condition or group."* (Gap 17: nested empty groups get only the `+`, no copy). Click → new `<InsertPicker>` popover with two items: **Condition** (opens existing `ConditionDrawer` create mode at `(parentId, index)`) and **Empty group ▸** (submenu picks AND/OR, inserts an empty group via `insertChild` and arms `pendingPicker` so the new group's empty-slot picker auto-opens — user is never left with a dangling empty group). Keyboard nav: ↑/↓ cycles items, Enter confirms, Esc closes (collapses submenu first if open), → opens the AND/OR submenu. Outside-click closes (mousedown capture phase). Editor wiring changes: `creating` state grows an optional `index?` field so create-mode preserves the click target through the drawer round-trip; `handleAddCondition(parentId, index?)` and new `handleAddGroup(parentId, index, logic)` both flow through `insertChild`'s existing index parameter. New `pendingPicker: {parentId, index} | null` + `consumePendingPicker(parentId, index)` are passed to Canvas → InsertSlot, which fires `setPickerOpen(true)` once via `useEffect` then clears the flag. Save validation now blocks `performSave` when `!hasAnyLeaf(tree)` (Gap 20) — corner pill removal means a user can stand up groups-only structurally-valid trees with no leaves, so the editor blocks the PUT before the backend's 422 catches it. Removed: corner `<AddPill onClick={onAddCondition}>condition</AddPill>` block and the `AddPill` component (no other callers, verified by grep). New `useTouchPointer()` hook subscribes to `matchMedia("(hover: none) and (pointer: coarse)")` for touch detection. Canvas extends its existing `onPointerMove` with `onCanvasPointerMove` to track world-coord pointer for proximity reveal (only on non-touch viewports, no allocations on touch). `lib/strategy/README.md` updated with `collectSlots` row in the helpers table and a new "Slot variants" subsection. Build green (15/15 pages), `tsc --noEmit` + `check-env.mjs` clean. Smoke testing on `/strategies/6` requires Google OAuth (deferred to user verification): expected behavior — empty strategy shows centered `+` with copy; populated tree shows `+` icons in gaps; clicking `+` opens picker; Empty group → AND submenu inserts a new dashed-border box and auto-pops its first-child picker — (2026-04-27)
- [x] **Condition tree — Phase C: canvas re-layout + boxed groups (visual rewrite, auto-layout).** New module `lib/strategy/layout.ts` runs a two-pass auto-layout: bottom-up `size()` computes each subtree's pixel bounding box, top-down `place()` assigns absolute `(x, y)` coordinates plus per-group `gateX`/`gateY` and outgoing-pin coords. Constants tuned so depth-1 strategies render pixel-near-identical to pre-Phase-C: `GAP=12` matches the old `NODE_GAP - NODE_H` sibling spacing, `ROOT_HGAP=170` puts the root gate at `x≈410` (the old `gateBlockX`). Nested groups use `INNER_HGAP=80` and `GROUP_PAD=18` so wires get clean Beziers at any depth. Single-child groups (n≤1) hide their gate; the only child's wire passes through to the grandparent's gate via `parentGateId` redirection in `place()`. `editor-view.tsx::Canvas` retyped to take `tree: ConditionGroup` (no more `leaves` flattening) — its inner IIFE now calls `layoutTree(tree)`, then renders three layers: an SVG with all wires (computed by walking `walkLeaves`/`walkGroups` and pushing `pushWire` paths against the precomputed `id → GroupLayout` map), HTML `<GroupBox>`es for nested groups (rounded rect, dashed outline default → solid hover → primary-light selected with outer glow, plus a `GROUP · AND/OR` mono label at the top-left), then `<CondNode>`s, then `<GateButton>`s last so they sit on top. New `<GateButton>` (replaces the inline gate `<button>`) is now used for both root and nested gates; clicking it selects the corresponding group. Selection: `SelKind` already included `"group"` in Phase B; both `GroupBox` border and `GateButton` route to `onSelect("group", id)` (Gap 13 — one selection target, two affordances). New `<GroupDrawer>` placeholder renders when `selection.kind === "group"`: shows the `GROUP · AND` header, an AND/OR radio group calling a generic `handleSetGroupLogic(id, logic)` (which delegates to `setGroupLogic` from tree.ts), and a contextual help line. Phase E will swap in Ungroup + Delete-group actions. Removed dead state: `flattenLeaves(tree)` derivation and `handleToggleRootLogic` are gone — both subsumed by the layout walker and the generic group-logic handler. `lib/strategy/README.md` updated with a `./layout.ts` section documenting the pass and constants. Build green (15/15 pages), `tsc --noEmit` + `check-env.mjs` clean. Smoke testing on `/strategies/6` requires Google OAuth so was deferred to user verification — depth-1 strategies should render pixel-near-identical at the gate position; the visual difference appears only when a nested group exists, which the editor cannot author until Phase E ships group creation — (2026-04-27)
- [x] **Condition tree — Phase B: frontend tree state + serialization (internal model swap, no visual change).** New module `lib/strategy/tree.ts` defines the in-memory recursive `ConditionGroup` / `ConditionLeaf` discriminated-union tree with stable client-side IDs (`crypto.randomUUID()`-prefixed), plus the immutable helpers (`findNode`, `findParent`, `insertChild`, `removeNode`, `replaceNode`, `setGroupLogic`, `flattenLeaves`, `depth`, `leafCount`, `hasAnyLeaf`) and the wire round-trip (`fromBackend`, `toBackend`, `normalizeWireGroup`). Editor swap: `editor-view.tsx` replaces `rules: RuleNode[] + logic: ConditionLogic` with a single `tree: ConditionGroup` state, hydrates from `initialStrategy.entry_rules.conditions` via `fromBackend` (legacy-tolerant: any node with a `logic` key is a group, otherwise a leaf — same heuristic the backend's `_backfill` uses), and `buildUpdateBody` now calls `toBackend(tree)` for entry and `normalizeWireGroup(exit.conditions)` for exit. CRUD handlers rewritten ID-first: `handleAddCondition(parentId)` (corner pill targets `tree.id`; Phase D's inline `+` will pass nested ids), `handleDeleteNode(id)` (uses `hasAnyLeaf` post-removal as the floor — replaces the old `rules.length <= 1` guard), `handleDuplicateNode(id)` (finds parent + index, inserts copy after via `insertChild`), `handleToggleRootLogic`. `selection` carries `CondId`. `Canvas` retyped: takes `leaves: ConditionLeaf[]` (flattened DFS) and `logic: ConditionLogic`; geometry math drives off `leaves.length`. Wire types in `lib/api/strategies.ts` tightened: `kind` is now required (`"condition" | "group"`) and `ConditionGroup.conditions` becomes `Array<SingleCondition | ConditionGroup>` (recursive union). Wizard already emits the canonical shape from Phase B.0 — no changes needed. Build green (15/15 pages), tsc + check-env clean. **Tree state is preserved through hydrate/save round-trips even though the canvas still flattens it visually** — Phase C is the rendering rewrite — (2026-04-27)
- [x] **Condition tree — Phase B.0: frontend emits `kind` discriminator on every payload.** Precursor PR before backend Phase A. Three small changes: (1) `lib/api/strategies.ts` adds optional `kind?: "condition"` and `kind?: "group"` fields to the existing flat types — kept optional so today's responses (which don't carry it) still validate; (2) `app/strategies/new/page.tsx::presetToEntryRules` injects `kind: "condition"` on each leaf and `kind: "group"` on the wrapping group across all six presets; (3) `app/strategies/[id]/editor-view.tsx::buildUpdateBody` rewrites both `entry_rules.conditions` and `exit_rules.conditions` (when set) to spread the `kind` field on emit. No runtime behavior change today — current backend's Pydantic `Extra.ignore` default silently drops the unknown key. Once Phase A backend lands with strict validation, this client is forward-compatible. Build green (15/15 pages), tsc clean — (2026-04-27)
- [x] **`STRATEGY_TREE_PLAN.md` — full plan for arbitrary-depth boolean condition trees.** User wants `(A AND B) OR (C AND D)`-style nested authoring with inline `+` insertion. Plan locks in 5 decisions (arbitrary depth, empty-group-then-fill flow, auto-layout no drag, boxed groups, phased rollout) and breaks the work into 5 shippable phases (A backend, B frontend state, C canvas, D inline add, E group editing). Backwards-compat falls out for free — flat strategies are valid depth-1 trees in both schema and renderer. Lifts the flat-schema constraint from ADR-10. **No code changes yet — plan only.** — (2026-04-27)
- [x] **Decorative-button audit — site-wide sweep, two stragglers fixed.** Followed up the `/signals` Filter+Export wiring with a full `<Btn>` + native `<button>` audit across `app/` and `components/` (57 `<Btn>` call sites, ~40 native buttons). Two true placeholders remained: (1) `app/strategies/strategies-view.tsx` empty-state "Import JSON" button — header copy at line 277 already had `onClick={onImport}`, but the empty-state copy at line 821 was forgotten; threaded the existing `onImport` prop through `EmptyState({ onImport })` (called from both branches of the `empty` ternary). (2) `app/signals/signals-view.tsx` per-row "chart" button — no chart route, modal, or `Sparkline` per-symbol view exists in the codebase, so the button had nothing to fire; removed entirely (cleaner than wiring to a stub). Every other `<Btn>` either has an `onClick` handler or is wrapped in a `<Link>` for navigation; every native `<button>` has a handler. Build green (15/15), tsc + check-env clean — (2026-04-26)
- [x] **`/signals` Filter + Export buttons wired (were design-only placeholders).** Both `<Btn>`s in the editorial-header `actions` slot were rendered without `onClick` during the Stitch redesign — visible but inert. `signals-view.tsx` now lifts `filter: FilterCriteria` and `filterOpen` state into `SignalsView`, derives `filteredSignals` via `useMemo(applyFilter, …)`, and threads them through `Body`. New `FilterModal` component with four controls — direction (All/Buy/Sell pill group), symbol contains (free-text input, case-insensitive substring match against `Signal.symbol`), RSI range (two clamped 0–100 number inputs), and a strategies pill toggle list (empty array = all). Reset/Cancel/Apply footer; the parent `handleApplyFilter` flashes "Filter applied" / "Filter cleared" via the existing `useFlash`. The Filter button styling flips from `ghost` → `primary` and shows a `●` glyph when any non-default criterion is set; the meta line shows "X of Y signals" when filtered, "Y signals" otherwise. Export wired to a new pure helper `exportSignalsCsv(signals)` that escapes `"` per RFC 4180, joins rows with `\n`, builds a `Blob` with `text/csv;charset=utf-8;`, and triggers a download named `signals-YYYY-MM-DD.csv` via an off-DOM `<a>` (cleaned up + URL revoked synchronously). Export operates on the *filtered* list and disables when empty; flashes "Exported N signals to CSV". No new deps, no backend touch — the strategy-driven feed already returns everything client-side. Build green (15/15 pages), `tsc --noEmit` + `check-env.mjs` clean — (2026-04-26)
- [x] **`/portfolio` G2 — null current_price now renders "—" instead of faking 0% P&L.** Both adapters (`page.tsx::mapPosition`, `portfolio-view.tsx::adaptPosition`) used to fall back `now=entry` when the backend returned `current_price=null` (no EOD row joined yet for that symbol), which made the row's unrealized pnl/return render as exactly 0.00 / +0.00% — indistinguishable from a real flat position. `OpenPosition.now` is now `number | null`; both adapters pass null through; `OpenPositionsTable` renders a muted italic em-dash for the now/value/pnl/return cells when null; the table builder produces null sentinels for those four columns; `Body` totals split positions into `pricedPositions` (now != null) and the rest, computes `totalCost`/`totalValue`/`unrealized`/`unrealizedPct` from priced only, and surfaces `N awaiting price` in the editorial header meta when any are unpriced; `buildSectors` filters unpriced (allocation by market value can't include rows with no defined value); `ClosePositionModal` defaults the exit field to `position.now ?? position.entry` so the close UX has a sensible numeric starting point even for unpriced rows; CSV export writes empty cell for null; CSV import maps empty/zero now_or_exit cells back to null (round-trippable). Audit gap **G2** closed; **G7** (signalEdgePkr accuracy) is moot — attribution is computed only from realized closed-trade pnl, never from unrealized open-position math, so unpriced rows never contributed to it. Build green (15/15), tsc clean — (2026-04-26)
- [x] **`/portfolio` G3 — closed-trades cap dropped.** `page.tsx` was passing `{limit: 100}` to `getClosedTrades`, which silently understated the "X closed" header count and the Realized YTD total as soon as a user crossed 100 closed trades. Backend `JournalService.list_closed_trades` already returns the full set when `limit` is omitted (the `LIMIT` clause is only applied when `limit > 0`). Cap removed; if render cost becomes a problem at scale, the future move is server-side pagination + `/portfolio/summary` for header totals (audit gap **G5**, still open). Audit gap **G3** closed — (2026-04-26)
- [x] **`/portfolio` G1 — sector allocation chart now uses real /stocks data.** Replaced the hardcoded ~25-entry `SECTOR_MAP` (only Oil & Gas, Cement, Banks, Fertilizer, Oil Marketing — everything else collapsed to "Other") with a `sectorMap: Record<symbol, sector_name>` built server-side in `app/portfolio/page.tsx` from the same `getAllStocks()` payload that already feeds the symbol autocomplete (zero extra network calls). Threaded through `PortfolioView` → `Body` → `buildSectors`. `SECTOR_COLOR` palette retuned to backend's actual sector strings ("Oil & Gas Exploration Companies", "Commercial Banks", etc.); unknown sectors fall back to `T.text3` (same treatment as the previous "Other" bucket). Build green (15/15 pages), tsc clean. Audit gap **G1** closed — (2026-04-26)
- [x] **Strategy editor PR-2 — CRUD + name edit + indicator picker (phases 5–7).** Builds on PR-1 to fully lift ADR-9 §2. `+ condition` AddPill now stages a draft `SingleCondition` (`indicator: rsi, operator: <, value: 50`) and opens the drawer in create mode; on Apply, appends to rules with id `r${Date.now()}`. The `+ group` pill is dropped (no nesting per ADR-10). Drawer Delete splices the rule (blocked when `rules.length === 1` with toast "A strategy needs at least one condition"); Duplicate clones with deep-copied value/params. Header name is now click-to-edit: hover surface lights up, Enter or blur commits, Escape reverts, empty input reverts to previous (backend min_length=1). Page server-component now `Promise.all`s `getStrategy()` + `getIndicatorMeta()` (hourly ISR, falls back to empty meta on failure) and threads the meta to `EditorView` → `ConditionDrawer`. `ConditionDrawer` gains an `indicator` Combobox (LHS) sourced from `meta.indicators`; the group name becomes the right-aligned hint. Re-introduces a constrained Period selector for parametric families (RSI fixed 14; SMA `[20,50,200]` mapping to `sma_20/50/200`; EMA `[12,26]` mapping to `ema_12/26`). When `compareMode === "Indicator"`, the read-only "next release" placeholder is replaced with a second Combobox for the RHS reference indicator. Free-typed text passes through (Combobox is permissive). Build green (15/15), tsc clean. ADR-9 §2 marked **LIFTED** in `PRE_AUTH_DECISIONS.md` — (2026-04-26)
- [x] **Strategy editor PR-1 — round-trip persistence (phases 1–4).** `editor-view.tsx` rewritten to lift canvas state into `EditorView` and treat `initialStrategy` as the source of truth. New top-level state: `rules: RuleNode[]`, `logic: ConditionLogic`, `exit: ExitRules`, `sizing: PositionSizing`, plus `dirty` + `saveStatus`. `Canvas` now renders N condition nodes programmatically (Y = 40 + i·120) with connectors auto-routed to a single central `GateGlyph` whose AND/OR is click-toggleable. Hardcoded `CONDITIONS` map, `GroupBox`, `GroupDrawer`, and the nested-OR visual all deleted (ADR-10). `ConditionDrawer` + `ExecutionDrawer` made controlled — they take live `cond`/`exit`/`sizing` props, expose `onApply`, and write back through parent state. Operator buttons constrained to backend's seven (`>`, `>=`, `<`, `<=`, `==`, `crosses_above`, `crosses_below`); decorative `=`/`·` dropped. Compare-mode flattened to `Constant` ↔ `Indicator` (Price-ref dropped); indicator-side picker stubbed read-only with a "next release" hint. Direction (long-only) and Exit-signal rows hidden — schema-less. New `parsePct`/`parseDays`/`fmtPct`/`fmtDays` helpers parse free-text exit fields back to numbers. New pure `buildUpdateBody(name, rules, logic, exit, sizing): StrategyUpdateBody` exported for unit testing. `handleSaveDraft` now PUTs `/api/strategies/{id}` with optimistic state, on success clears `dirty` + sets `savedAt`, on error flips `saveStatus="error"` and re-enables a "Retry save" button. Header reads `dirty` + `saveStatus` and renders one of `saved Xm ago` / `unsaved changes` / `saving…` / `save failed — retry` with corresponding color. Build green (15/15 pages), `tsc --noEmit` clean. Lifts ADR-9 §2 partially. **User-facing trigger**: 2026-04-26 report — *"changed symbol and saved for a node but it didn't persist"* — (2026-04-26)
- [x] **`/portfolio` LogTradeModal — symbol + strategy autocomplete.** New `Combobox` atom (`components/atoms.tsx`) — permissive: input is always free-text, dropdown is a hint, free-typed values pass through unchanged so a stale cache or mistyped ticker can't block submission. Arrow-key nav, Enter/Tab to accept, Escape to close, click outside to dismiss. Filtering is case-insensitive prefix-then-contains over `label` + `keywords`. Server component (`portfolio/page.tsx`) now also fetches `getAllStocks()` (paginated walk of `/stocks`, 5min ISR per page) and `getStrategies(jwt)` in parallel; both wrapped in `.catch()` so a free-tier 403 on strategies or a transient stocks failure degrades to an empty list instead of breaking `/portfolio`. New file: `lib/api/stocks.ts`. — (2026-04-26)
- [x] **Backend wiring — Phases 3.2b/c, 3.3, 3.4 API surface, 3.5 — full app stretch in one session.** Each route ships safe read paths + safe writes; complex round-trips deferred per ADR-9. Files added/changed:
  - **API client modules**: `lib/api/strategies.ts` (extended with backtest types/wrappers), `lib/api/bots.ts` (full CRUD + positions/trades/performance), `lib/api/portfolio.ts` (summary/orders/trades/place-order/reset/add-funds).
  - **Server-side proxy routes** (mint JWT from session, forward to backend): `/api/strategies` (POST), `/api/strategies/[id]` (GET/PUT/DELETE), `/api/strategies/[id]/backtest` (POST), `/api/strategies/[id]/backtest/job/[jobId]` (GET poll), `/api/bots` (POST), `/api/bots/[id]` (PUT/DELETE), `/api/bots/[id]/[action]` (start/pause/stop), `/api/portfolio/orders` (POST), `/api/portfolio/reset` (POST), `/api/portfolio/add-funds` (POST).
  - **Phase 3.2b — `/strategies/new` wizard**: final-step button now POSTs `StrategyCreate` (built by `presetToEntryRules` + `universeToFilters`) and routes to `/strategies/{id}` or `/backtest?strategy_id={id}` per the After-creating choice. Pro-plan-required errors render inline.
  - **Phase 3.2c — `/strategies/[id]` editor**: split into `editor-view.tsx` + server `page.tsx`. Server fetches the strategy by id; view replaces hardcoded display strings (`rsi_bounce_v1`, `RSI Bounce v1`, `mean reversion · KSE-100`, `+14.2%`, `3 signals today`) with real values. Deploy/pause toggle now PUTs `{status: ACTIVE|PAUSED}` with optimistic flip + rollback on error. Save-draft remains local-only (canvas serialization deferred — ADR-9 §2).
  - **Phase 3.3 — `/bots`**: list/wizard/dashboard all wired. List server-fetches `GET /bots`, maps `BotResponse → BotRow`, splits into `bots-view.tsx`; Pause-all/Resume-all fire per-bot POSTs via `Promise.allSettled` and `router.refresh()`. Wizard reads `?strategy_id=N` from URL, POSTs `BotCreate` body, routes to `/bots/{id}` (Pro+ required). Dashboard server-fetches bot + open positions + 30-day performance in parallel with `Promise.allSettled`; replaces all hardcoded copy/charts/tables (Lede tiles, EquityCurve, OpenPositions table, "recent activity" panel) with real data and empty-states. Pause/Resume button hits `/api/bots/{id}/{pause|start}`. Logs modal shows "structured log streaming — coming soon" (no backend log endpoint — ADR-9 §3).
  - **Phase 3.4 — `/portfolio` API surface only**: full type/wrapper surface in `lib/api/portfolio.ts` and three `/api/portfolio/*` route handlers. The page itself is **untouched** — its journal/CSV semantics differ from the backend's paper-trading semantics, and naïvely wiring would change product behavior (ADR-9 §5).
  - **Phase 3.5 — `/backtest`**: split into `backtest-view.tsx` + server `page.tsx`. Reads `?strategy_id=N` (required) and optional `?backtest_id=M` from URL; server-fetches strategy name + backtest result (specific id, or latest, or empty). Lede tiles / EquityCurve / TradeLog / breadcrumb / date-range / capital all driven by `BacktestResultResponse`. Re-run button POSTs `/api/strategies/{id}/backtest` (12-month range, 1M PKR — ADR-9 §6), polls `/api/strategies/{id}/backtest/job/{jobId}` every 1.5s for up to ~45s, syncs URL with `?backtest_id={id}` on completion, calls `router.refresh()`. Sync-mode (Redis off) returns the full result inline. Deploy/Pause action also wired to PUT strategy status.
  - **Build**: 17/17 pages clean. New dynamic routes: `/api/*` (8 handlers), `/backtest`, `/bots/[id]`. Page tree expanded from 12→17 routes.
  - **ADR-9** added to `PRE_AUTH_DECISIONS.md` consolidating all six deferrals across Phase 3.2b–3.5 with explicit lift triggers — (2026-04-26)
- [x] **Backend wiring — Phase 3.2a: `/strategies` list slice** — `app/lib/api/strategies.ts` types-mirror `backend/app/schemas/strategy.py` (full CRUD types + meta-endpoint types pre-staged so wizard/editor follow-ups don't duplicate work). List page split: `app/strategies/page.tsx` is now a server component that `auth()` → mints JWT → calls `getStrategies({page:1, page_size:100})` → maps `StrategyResponse → Strategy` and forwards to the new `app/strategies/strategies-view.tsx` client island. Mapping rules: `id` int → string, `ACTIVE → "DEPLOYED"`, `updated_at` → relative `Xm/h/d/mo ago`, `universe` derived from `stock_symbols` / `stock_filters.sectors` / `"KSE-100"` fallback. **Per-row enrichment deferred** (signals-today, latest backtest %, Sharpe, output bindings) — see ADR-8 in `PRE_AUTH_DECISIONS.md`: rendering as empty sentinels (0/—/null/[]) trades 1 round trip for 2N+1, with the explicit trigger to lift the deferral being either user feedback or a backend `?include=summary` flag. Existing JSON-import flow stays in-memory only; nothing POSTs from the list page. Build flips `/strategies` from `○` to `ƒ` (dynamic) — same shape as `/signals`. 12/12 pages clean — (2026-04-26)
- [x] **Verification pass + 3 bug fixes after Phases 0–3.1** — (1) `MarketingNav` now reads `?auth=required` on mount and auto-opens AuthModal, then strips the query via `history.replaceState` so refresh doesn't re-trigger. Reads `window.location` inside `useEffect` (not `useSearchParams()`) to keep `/brand` and other static-prerendered marketing pages from bailing out of SSG. (2) Pricing fallback constants in `pricing-view.tsx` aligned to seeded backend rows: `FALLBACK_MONTHLY = { pro: 1500, quant: 3000 }`, `FALLBACK_DISCOUNT = 1 - 15000/(1500*12) ≈ 0.1667`. Yearly billing toggle copy now reads "save 17%" to match real seeded discount instead of the aspirational "save 20%". (3) `package.json` gains a `lint` script aliasing `tsc --noEmit && node scripts/check-env.mjs` so the parent project's pre-commit checklist runs without adding ESLint (no-ESLint decision preserved). Build still 12/12 pages, lint clean — (2026-04-26)
- [x] **Backend wiring — Phase 3.1: `/pricing`** — public, single `GET /subscriptions/plans` call. New `lib/api/subscriptions.ts` (no JWT, ISR `next: { revalidate: 3600 }`). Server `page.tsx` fetches plans, maps backend's `pro` / `pro_plus` rows by `name + interval` to the design's `pro` / `quant` tier slots, falls back silently to design defaults if the fetch fails — public marketing page must not crash on backend hiccups. Client `pricing-view.tsx` keeps every existing inline-style markup, blurb, FAQ, and comparison row; the `tierPricing()` helper now derives the yearly footnote and discount % from real prices and computes them dynamically. Build keeps `/pricing` static (`○`) — ISR handles refresh — (2026-04-26)
- [x] **Backend wiring — Phase 2: `/signals` end-to-end spike** — first live backend integration. New `app/lib/api/jwt.ts` mints HS256 JWTs from the NextAuth session via `node:crypto` HMAC (no new deps; FastAPI's `verify_jwt` accepts `aud="authenticated"`). `app/lib/api/client.ts` exposes `apiFetch<T>(path, { jwt, ... })` with typed `ApiError` on non-2xx. `app/lib/api/signals.ts` types-mirror `backend/app/schemas/signal.py`. `/signals` page split into a server component (`page.tsx`: `auth()` → mint JWT → `getSignals()` → map `TradingSignalWithSymbol → Signal`) and a client island (`signals-view.tsx`: existing markup verbatim, no SWR). Build flips `/signals` from static `○` to dynamic `ƒ`. Errors propagate to existing `app/error.tsx` — (2026-04-26)
- [x] **Backend wiring — Phase 1: Auth (NextAuth v5 / Google)** — `next-auth@5.0.0-beta.31` pinned (only new runtime dep). `app/auth.ts` ports `psx-trading-view/auth.ts` verbatim, preserving the `account.providerAccountId → token.user_id` mapping that existing Neon DB user rows key off. Route handler at `app/app/api/auth/[...nextauth]/route.ts`, gated proxy at `app/proxy.ts` (Next.js 16 file-convention rename of `middleware.ts`) protecting `/signals /strategies /backtest /bots /portfolio` and redirecting to `/?auth=required&from=<path>`. AuthModal Google button now calls `signIn("google", { callbackUrl })` — pending placeholder branch removed. `app/.env.example` documents `NEXTAUTH_URL`, `NEXTAUTH_SECRET`, `GOOGLE_CLIENT_ID/SECRET`, `NEXT_PUBLIC_API_BASE_URL`. ADR-7 added to `PRE_AUTH_DECISIONS.md`. Build clean, `check-env.mjs` passes, no UI markup changes — (2026-04-25)
- [x] **Phase 0 — git repo initialized and pushed** to `https://github.com/Simba256/PSXAlgos-Frontend.git` (main branch). Stale `app/.git` removed; root-level `.gitignore` added (covers OS noise, `.claude/`, env files with `!.env.example` exception, root-level PNGs); `app/.gitignore` already path-anchored. 59 files in initial commit, no Claude branding — (2026-04-25)
- [x] Repo cleanup — removed ephemeral PNGs, `.playwright-mcp/`, 5 angle-review reports, closed survey/followup docs, and pre-app design scaffolds. Repo root now: `PROJECT_TRACKER.md`, `FRONTEND_REVIEW_2026-04-24.md`, `PRE_AUTH_DECISIONS.md`, `FUTURE_TRIGGERS.md`, `app/` — (2026-04-25)
- [x] P3 trigger registry — 8 earned-at-signal items at `FUTURE_TRIGGERS.md` (Lede clamping, TerminalTable virtualization, signal-log-bridge retirement, real-fetch loading states, Step 3 name truncation, Help nav placement, RTL audit, print stylesheet). Trigger fires → entry moves here, deleted from registry — (2026-04-25)
- [x] P2 bucket — 6 ADRs at `PRE_AUTH_DECISIONS.md` (session tokens, OAuth redirect allowlist, action-tier blast radius, env-leak rule, analytics scrub, CSS-var escape valve). Two implementations shipped: `app/scripts/check-env.mjs` wired to prebuild; `app/globals.css` `:root` + `[data-theme]` token system. ADR-7 (form primitives) deferred — wizards use `onClick`, not `<form>` — (2026-04-25)
- [x] P1 bucket — 18 of 20 applied: security headers (CSP deferred to auth), prod-gated `allowedDevOrigins`, `reactCompiler: true`, contrast fixes (Paper text3/primaryLight), SSR theme-flash eliminated, mobile-first `useSyncExternalStore` breakpoint, `<Modal>` + `<FlashToast>` primitives swapped at 10 sites, portfolio CSV extracted to `lib/portfolio-csv.ts`, a11y micro-fixes (`100dvh`, `aria-hidden` SVG sweep, target-size bumps, semantic landmarks + skip-link). P1-16 hamburger threshold rejected; P1-17 Lede overflow deferred to P3 — (2026-04-25)
- [x] P0 bucket — 6 bugs fixed: `/brand` `PAD[bp]` TS error; strategies import dedup (no more `Math.random()` IDs); site-wide `:focus-visible`; canvas-node keyboard activation in `strategies/[id]`; portfolio CSV reads from real positions (not `forceEmpty`-gated); bots stale-closure on `runningCount` — (2026-04-24)
- [x] 5-agent frontend review — parallel code-quality / a11y / perf / security / layout audits consolidated to `FRONTEND_REVIEW_2026-04-24.md` with P0–P4 bucket synthesis. Predicted Web Vitals: LCP 1.8–2.6s, CLS 0.05–0.15 — (2026-04-24)
- [x] Frontend survey + consistency followup work — 8 survey fixes (`/pricing` mobile stack, `/bots/new` fluid sizing, `/bots/[id]` Lede clamp, `/signals` nowrap default, `/backtest` subgrid, ordinal-span hardening) + 4 followup items (portfolio Allocation-by-sector subgrid, `/portfolio` Lede strip clamped, prophylactic grid sweep deferred per evidence-bar rule). Established patterns now codified in `FUTURE_TRIGGERS.md` — (2026-04-24)
- [x] Google-only auth modal wired to marketing nav (`components/auth-modal.tsx` + `MarketingNav`) — single-step modal, theme-inverted Google button for max contrast, ESC + click-outside + focus scope + body scroll-lock + `psx-fade-in`/`psx-pop-in` animations. Pending state until OAuth backend lands — (2026-04-24)
- [x] Final logo system — α-signed mark with node-graph signature ("V20"); single SVG flips colors via `T.primary`/`T.surface` per theme; full icon resolution set (favicons, apple-touch, maskable 192/512); PWA manifest at `public/manifest.webmanifest`; favicon `<link>` retargets at runtime to track site theme (not OS preference); internal `/brand` lab catalogs 25 explored variants — (2026-04-23)
- [x] Site-wide responsive support — `useBreakpoint` (640 / 1024 tokens) in `components/responsive.tsx`, hamburger drawer for marketing + app navs, mobile card-view for all 7 data tables in `TerminalTable`, Pointer-Events-driven pan + pinch-zoom on the strategy editor canvas, hardened `globals.css` (overflow, iOS auto-zoom guard, `prefers-reduced-motion`). Verified across 11 routes × 4 viewports — (2026-04-22)

## Blockers
- None

## Key Decisions
- (2026-04-27) **Condition trees become arbitrary-depth, not flat** (`STRATEGY_TREE_PLAN.md`). Reverses the flat-only stance from ADR-10. Backend `ConditionGroup.conditions` becomes a discriminated `Union[SingleCondition, ConditionGroup]` (recursive, `kind: "condition" | "group"` field via `Field(discriminator="kind")`); evaluator recurses. Frontend swaps `rules[] + logic` for a `ConditionNode` discriminated-union tree with auto-layout and boxed group rendering. Inline `+` everywhere replaces the corner pill. Five phases (A backend → B FE state → C canvas → D inline-add → E group-edit), each independently shippable. Legacy strategies (no `kind`) are structurally valid depth-1 trees; a read-time normalizer at the strategies router boundary backfills `kind` before Pydantic validation, keeping signal scanner / backtester / bot engine seamless during rollout. ADR-10 is partially lifted — the *flat-schema* premise no longer holds, but the schema-less-UI hiding (timeframe, direction, exit-tree) stays out of scope.
- (2026-04-26) **Strategy editor canvas wires to the flat schema** (ADR-10 + `STRATEGY_EDITOR_WIRING_PLAN.md`). The backend's `EntryRules` is a single flat `ConditionGroup{logic, conditions[]}` — no nesting. The canvas's nested OR-group visual is therefore unrepresentable; it gets dropped, replaced with N rule nodes feeding one AND/OR gate. Save model is **explicit Save Draft only** (drawer Apply marks dirty; header Save Draft is the one PUT). Schema-less UI fields (Long/Short/Either direction, per-condition Timeframe, separate-tree Exit signal) are hidden — shipping fields the backend can't honor is worse than shipping nothing. Indicator periods become constrained dropdowns (SMA `[20,50,200]`, EMA `[12,26]`, RSI static, others hidden) because the indicator vocabulary is enum-scoped, not parametric. Work splits into two PRs: PR-1 (round-trip existing rules) before PR-2 (CRUD + name edit + indicator picker). Lifts ADR-9 §2.
- (2026-04-26) **Phase 3.2b–3.5 ship safe writes only** (ADR-9). Wizard preset→entry_rules is starter conditions (refine in editor); editor canvas does NOT round-trip (only deploy/pause toggle persists); bot dashboard activity log is empty-state until backend grows a log endpoint; portfolio backend semantics (paper-trading) differ from journal UI semantics so the page stays on local CSV with the API surface ready for the next slice; backtest Re-run uses opinionated 12mo / 1M PKR range. Each deferral has an explicit lift trigger documented in ADR-9. The full type + route surface still ships so follow-ups don't re-derive contracts.
- (2026-04-26) **Server-side proxy routes (`app/api/*`) are the canonical pattern for client→backend writes**. The browser never sees the backend JWT; route handlers mint it from the NextAuth session via `auth()` + `signBackendJwt()` and forward to FastAPI. Each handler has a small fixed shape: parse params, get JWT, call the typed wrapper, map `ApiError` to JSON with the same status. This decouples client code from auth and keeps the backend URL behind one env var (`NEXT_PUBLIC_API_BASE_URL`).
- (2026-04-26) **`/strategies` list ships without per-row enrichment** (ADR-8). The frontend's `Strategy` row shape includes `signals` (today), `bt` (latest backtest %), `sharpe`, and `outputs` glyphs; none are on the bare `StrategyResponse`. Sourcing them costs 2N+1 round trips for the index page of authoring work. List slice ships with empty sentinels; enrichment lifts when users with ≥10 strategies report it as a workflow problem, OR when the backend grows a `?include=summary` flag. `type` is hardcoded `"Custom"` (the frontend's mean-rev/momentum/trend taxonomy doesn't exist server-side and would mis-classify multi-indicator strategies). `universe` is derived locally from `stock_symbols` or `stock_filters.sectors`.
- (2026-04-26) **HS256 JWT minted with `node:crypto`, not `jose`** — psx-ui's package.json keeps its hard-line minimalism (`next-auth` is the only new runtime dep). HS256 is HMAC-SHA256 + base64url; ~30 lines of stdlib code, edge-of-correctness-zero. `jose` is transitively present via `next-auth` but importing transitives couples us to `next-auth`'s internal choices. Server-only guard in `lib/api/jwt.ts` throws if loaded in the browser.
- (2026-04-26) **`/signals` page split: server component fetches, client island renders.** Server component reads session, mints JWT, calls backend, maps response, passes data into `<SignalsView initialSignals={…} />`. The client island (`signals-view.tsx`) keeps every byte of the existing inline-style markup unchanged. This is the canonical Next.js 16 pattern for protected data routes and avoids needing SWR or a client-side `/api/auth/token` endpoint for first paint. Refetch-on-focus and polling get added per-route only when their need is observed.
- (2026-04-25) **NextAuth v5 chosen over hand-rolled OAuth** (ADR-7). The 3-runtime-dep purity protects against UI bloat (Tailwind, shadcn, Radix, SWR), not auth infra. Two facts forced NextAuth: (1) the FastAPI backend already verifies NextAuth-issued HS256 JWTs and the live Neon DB has user rows keyed off `account.providerAccountId`, so hand-rolling would orphan accounts; (2) the previous frontend's 34-line integration ports verbatim. `next-auth@5.0.0-beta.31` is the only new runtime dep in the entire migration.
- (2026-04-25) **`proxy.ts`, not `middleware.ts`** — Next.js 16 (`16.2.4`) renamed the file convention. The previous name still works but emits a deprecation warning at build time. Using the new name keeps the build clean and survives the eventual removal.
- (2026-04-25) **Pre-auth architecture documented as ADRs at `PRE_AUTH_DECISIONS.md`** (6 ADRs). Two have code shipped today; the other four fire when their trigger event arrives. Rationale: cost of writing 6 ADRs now ≈ 20 min; cost of reversing wrong choices later = migration work, security incidents, compliance review.
- (2026-04-25) **No ESLint in the project** — the `NEXT_PUBLIC_` leak guard is a 150-line `scripts/check-env.mjs` wired to `prebuild`, not an ESLint rule. Project ships 3 runtime + 5 dev deps and deliberately has no linter; a deps-free Node script catches the same class of bugs at the same lifecycle point.
- (2026-04-24) **Auth is Google-only, single flow, modal (no dedicated route)**. One "Continue with Google" button creates account if new, signs in if returning. Modal over the marketing site keeps landing context intact and avoids a sparse auth page round-trip. Apple ID / magic-link can be added later if analytics demand it.
- (2026-04-23) **Final logo: α with node-graph signature ("V20 α signed")**. α is name-rooted ("a" of Algos), finance-rooted (alpha = excess return), and the node signature preserves the visual-strategy-editor story. No competitor (KTrade, Sarmaaya, Finqalab, QuantConnect, Composer) uses α — uncontested category real estate.
- (2026-04-23) **Single mark, theme-reactive fills** (not two separate logos). The app is already runtime-themed via `useT()`, so the logo joins the same token system — geometry is the identity, colors come from `T.primary`/`T.surface` and flip automatically.
- (2026-04-22) **Responsive breakpoints fixed at 640 / 1024**. Marketing nav doesn't fit below 1024, app nav is data-dense; both switch to hamburger at the same threshold so behavior is consistent.
- (2026-04-22) **Runtime `useBreakpoint` over CSS media queries**. Codebase is entirely inline-style React — a hook fits the existing idiom and lets conditional rendering share the same breakpoint logic as layout.
- (2026-04-22) **Card view on mobile for all data tables, desktop table unchanged**. NN/g consensus: comparison/record-list tables on mobile should become cards. Implementation lives inside `TerminalTable`, so per-page changes are 1–3 flag additions on existing `cols` arrays.

## Notes
- App lives at `/app/`; this tracker and source repo root are at `/media/shared/personalProjects/psx-ui/`.
- Next.js version: 16.2.4. See `app/AGENTS.md` — some APIs may differ from older docs; consult `app/node_modules/next/dist/docs/` before touching framework-level code.
